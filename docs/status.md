# Seatgeist Status

Checklist tracker: `docs/tracker.md` is the renamed `Seatgeist Tracker` board and records done, partial, and remaining work using `[x]`, `[~]`, and `[ ]` against the full Seatgeist project scope.

Release checklist: `docs/release-checklist.md` records public-release blocking evidence, headless CI scope, and release-cut steps.

Unsupported paths: `docs/unsupported-paths.md` records what the current KDE-first release does not support yet.

Naming: the public project name is `Seatgeist`. Binaries, crates, service names, plugin config, runtime paths, and MCP tools now use the `seatgeist-*` / `seatgeist.*` identity.

## 2026-07-30 policy and lifecycle follow-up

- Protected applications are omitted from window inventories, active-window
  metadata, observations, post-action context, bridge counts, and journal-tail
  window context. Focused and unscoped AT-SPI reads are denied while a protected
  app is active. Screen captures dynamically black-fill protected window
  geometry; ambiguous portal window/area captures fail closed instead of
  exposing pixels.
- KWin bridge ownership recovery uses non-replacing name requests with bounded
  exponential backoff and exposes retry count, delay, and a compact last error
  in bridge status. Script and agent-seat action delivery use daemon-side long
  polling with watchdog timers, replacing the 50 ms idle polling loops.
- `seatgeist-cli input status` now reports aggregate backend selection.
  `seatgeist-cli input uinput-status` is the explicit `/dev/uinput` diagnostic;
  `input backends` remains an alias for compatibility.
- AT-SPI quality status reports same-user registry process counts and flags
  extra processes as possible stale accessibility-bus generations.

## 2026-07-27 operational hardening

- Focus dispatch is no longer reported as success unless the requested window is
  observed as active. A timed-out default focus settlement returns a failed
  action to CLI/MCP callers and is journaled as failed.
- AT-SPI readiness now connects to the advertised accessibility bus and probes
  the Registry root instead of treating a published but stale socket address as
  available. It honors an explicit `AT_SPI_BUS_ADDRESS` for isolated
  accessibility tests and identifies an advertised but unreachable Unix socket
  as a likely test-broker collision. Headless tests disable the bridge and use a
  private `XDG_RUNTIME_DIR`; accessibility-enabled fixtures must additionally
  use their own bus address. External diagnostic commands have all standard streams detached,
  preventing expected probe failures from polluting daemon logs.
- Health responses expose protocol version, daemon run id, build timestamp and
  source/hash provenance, plus an effective-config fingerprint. Deployment
  verifies the running executable hash after restart. Journal entries carry the
  run/build ids and include daemon start/stop lifecycle records.
- Errors include a stable `reason_code` in addition to their broad `kind`.
  KeePassXC is a built-in protected application, configured deny entries extend
  that list, and app-policy failures remain `app_denied` across focus,
  keyboard, pointer, accessibility, and exact capture targeting. MCP renders
  these as explicit non-retryable policy denials.
- The KWin bridge name is non-replaceable, ownership is checked and reacquired,
  and action capability readiness expires with the script
  heartbeat. Bridge status reports snapshot ages and staleness rather than a
  cached startup boolean. Owner loss therefore disables window actions
  immediately instead of producing a three-second unknown timeout.
- The agent-seat KWin plugin stops polling when the bridge owner disappears,
  removing the ownerless D-Bus log storm. Default independent-seat settlement
  uses the compositor delivery acknowledgment rather than generic desktop
  change polling.
- Narrow KWin script/plugin reload tooling verifies ABI and post-load state and
  always refuses an in-place compositor restart.
- MCP removes timestamped runtime screenshots after successfully embedding
  their image bytes. Explicit output paths and metadata-only requests remain
  persistent. The user service uses bounded restart bursts and stepped backoff
  instead of an unbounded one-second restart loop.

## 2026-07-18 agentic hardening

- The installed Codex plugin defaults to the six-tool core MCP profile. The local-install checker hashes the full source and installed trees, and the refresh target uses the plugin cachebuster helper before reinstalling, so a stale cached skill or tool contract fails explicitly.
- Exact KWin capture sessions are owner-scoped and may coexist across MCP processes; portal chooser-backed capture remains globally serialized, and all seat input still uses the single daemon seat lease.
- Readiness reports actionable state enums and an opaque active-desktop guard revision. Core window-session inventory returns a revision and bounded change wait, removing repeated list/active-window model polling.
- Inventory issues 10-second, owner-bound, one-shot semantic window handles. High-level semantic actions consume the handle, then resolve, correlate, policy-check, and invoke the AT-SPI target in the same daemon action.
- Action dispatch is reported separately from confirmation evidence. Journal error summaries are kind-only by default, with full diagnostic text available only through the private-run `journal.include_error_details` opt-in.

## 2026-07-04

Phase 0 scaffold is present:

- Cargo virtual workspace with resolver 3.
- Rust 2024 member crates for shared types, policy, backend traits, daemon, CLI, MCP, KWin, AT-SPI, and testkit.
- Conservative plugin, MCP, hook, skill, systemd, udev, and polkit skeletons.
- The initial CLI and daemon scaffold has been replaced by the implementation slices below.

Phase 1 first slice is implemented:

- `seatgeistd` binds a Unix socket, enforces restrictive socket directory/socket permissions, and rejects clients from another UID using Unix peer credentials.
- The daemon serves newline-delimited JSON requests for `health`, `capabilities`, and `policy_status`.
- `seatgeist-cli doctor`, `capabilities`, and `policy-status` call the daemon over the Unix socket.
- `make smoke` starts a temporary daemon, calls the CLI health/capability/policy/session/readiness/journal commands, verifies socket directory/socket/journal modes, and is included in `make verify`.
- The daemon and CLI can capture a full-screen PNG through Spectacle when run in the host KDE session. The smoke capture on this workstation returned a 7680x4320 source image.
- Screenshot output now defaults to a bounded preview. On the 8K workstation, the default CLI screenshot produced a 1600x900 PNG with source/output dimensions and scale metadata. Full-resolution screenshot requests are explicit and policy-gated separately; they prompt/fail closed by default until the daemon is started with full-resolution screenshot approval.
- Direct CLI screenshot commands and MCP screenshot tools now have a safe default output location: `seatgeist-cli screenshot`, `screenshot-tile`, `wait-for-change`, MCP `seatgeist.screenshot`, `seatgeist.screenshot_tile`, and `seatgeist.wait_for_change` write timestamped PNGs under `$XDG_RUNTIME_DIR/seatgeist/screenshots/` or the daemon-compatible `/run/user/<uid>/seatgeist/screenshots/` fallback when the output path is omitted.
- Spectacle captures are serialized inside the daemon because concurrent Spectacle captures can race.
- `seatgeist-cli monitors` now reports KWin monitor metadata from `org.kde.KWin.supportInformation`; on this workstation it reports `HDMI-A-2` as 5120x2880 logical at scale 1.5, matching the 7680x4320 screenshot source.
- Screenshot responses include the same monitor metadata when KWin responds.
- `seatgeist-cli screenshot-tile` can crop a physical-pixel region from a full-source capture and optionally downscale the tile. It prefers the portal Screenshot source when available, falls back to Spectacle when the portal backend fails before user cancellation, and preserves backend provenance in the response. A host smoke captured a 1600x1200 tile at source origin 3200,1600 and wrote an 800x600 PNG with scale factors 0.5.
- `seatgeist-cli windows` lists open windows through KWin's `WindowsRunner`, deduplicates repeated matches by stable KWin id, and enriches each id through `org.kde.KWin.getWindowInfo` for PID, title, app id, logical geometry, and monitor association derived from KWin monitor overlap. When the Seatgeist KWin script bridge has published a window-list snapshot, the daemon merges bridge metadata into that list; the public `getWindowInfo` PID is the documented fallback when the private script API omits `window.pid`, and target binding still fails closed if both paths omit it. If the runner path fails after a bridge snapshot exists, the daemon falls back to the bridge list.
- `kwin/seatgeist-bridge` packages the Seatgeist KWin script. It reads KWin's `workspace.activeWindow` and `workspace.stackingOrder`, subscribes to window activation/add/remove signals, and publishes compact active-window plus window-list JSON to the daemon over the session bus. A bounded, configurable two-second `QTimer` heartbeat republishes the snapshot after daemon-only restarts; startup/event publication remains the fallback when a KWin runtime lacks the timer. `make validate-kwin-bridge` covers this behavior in a mocked script runtime without touching the live desktop.
- `seatgeist-cli active-window` reads the daemon's latest KWin script bridge update. Before the script reports its first update, the command still fails with the documented bridge requirement because KWin's interactive `queryWindowInfo` is not suitable for unattended active-window checks.
- `make smoke-windows` validates window listing in a host KDE session and accepts either a real active-window bridge response or the documented bridge-not-yet-reporting failure.
- `make install-kwin-script` is available as an explicit, opt-in KWin configuration mutation for installing/enabling the script. Its modular installer now performs a targeted unload/load/run of only `seatgeist-bridge` when live KWin scripting is available, ensuring package updates replace the running JavaScript; outside KDE it defers loading to the next session. Fake-command tests cover both paths.
- `make install-kdeconnect-kwin-recovery-user` installs a user-scoped lifecycle bridge for KDE Connect remote input. It routes D-Bus activation into the generated managed autostart unit, orders it after KWin and the KDE portal, and asynchronously restarts only an already-active KDE Connect daemon after future KWin starts so stale RemoteDesktop/EIS sessions cannot survive a compositor generation change. The installer test validates install/remove assets without touching the live desktop.
- The KWin script was installed on this workstation and a host smoke observed a real active window with app id, pid when published by the bridge, logical geometry, and derived monitor association through the daemon bridge.
- The daemon writes compact JSONL journal entries for every handled request. Entries include method, success, optional explicit client tool identity from current CLI/MCP request envelopes, optional best-effort client pid/process-name metadata from Unix peer credentials, safety class, guard presence, summary, best-effort active-window before/after context for control-class requests, and optional structured control metadata with action id, policy result, backend provenance, and redacted requested-target fields. The journal records lengths, coordinates, offsets, node ids, and backend hints where useful, but not typed text, replacement text, clipboard contents, screenshots, or semantic target names. Screenshot-bearing entries can include opt-in artifact path, byte-count, and SHA-256 metadata when `[journal].include_artifact_metadata = true`; this is off by default and never stores image payloads in the journal. `seatgeist-cli journal tail --limit N` reads recent entries through the daemon, and smoke tests verify restrictive journal file permissions.
- Daemon requests now pass through the policy engine before execution. Current observe/status requests are allowed by default; prompt-level requests fail closed unless a configured approval file contains a matching unexpired method/safety-class grant.
- `seatgeistd` now reads optional daemon path, approval-file path, private capture restore-token path, requested raw input backend, explicit EIS XKB keymap RMLVO names, policy defaults including destructive-action and secret-field policy, app allow/deny lists, `journal.include_artifact_metadata`, `safety.require_focus_guard`, `safety.pause_on_human_input`, and `safety.redact_regions` from `~/.config/seatgeist/config.toml` or `--config` / `SEATGEIST_CONFIG`. CLI path arguments, `--capture-restore-file` / `SEATGEIST_CAPTURE_RESTORE_FILE`, `--input-backend` / `SEATGEIST_INPUT_BACKEND`, and explicit approval flags remain higher precedence than file values. `seatgeist-cli approve` appends short-lived JSONL grants for the configured approval file; the daemon rejects insecure approval files. App deny/allow rules, required active-window guards, secret-looking high-level text fields, and fresh human-input activity signals apply before backend execution; configured screenshot redaction regions are black-filled before screenshot outputs leave the daemon.
- Ordinary list-window, active-window, and direct-focus requests now use the shared injected `WindowBackend`. Its production KWin adapter owns runner/bridge merging and monitor correlation and delegates focus to the internal KWin executor. `seatgeist-cli focus --window <id>` remains a control action: default policy rejects it until a matching approval-file grant exists, while `seatgeistd --allow-control` remains available for explicit broad local smoke/manual use.
- First-class `move_window` and `resize_window` now use the same shared `WindowBackend`: the daemon authorizes the exact destination app, journals requested geometry, and waits for the packaged KWin bridge to acknowledge actual `frameGeometry`; no pointer-drag fallback exists. `launch_window` is desktop-entry-only and arms a bounded KWin one-shot intent before `gtk-launch`, then confirms panel-aware anchored placement and the requested focus policy. CLI, expert MCP, and core `seatgeist.act` share these policy and journal paths. Guarded `page_zoom` remains available for Firefox and Chromium-family windows, reporting browser-defined zoom steps rather than an assumed exact percentage.
- A desktop entry that reuses an already-running single-instance application, as Firefox commonly does, cannot satisfy `launch_window`'s exact-new-window contract. The compositor timeout is now classified as validation reason `launch_no_new_window` rather than a generic backend failure; callers should retain and address an existing exact window instead of retrying the launch.
- MCP desktop control now preserves the user's physical focus: `focus_window` is no longer advertised to models, daemon-side MCP focus attempts are rejected, and MCP launches accept only `preserve_focus`. Exact `close_window` is session-bound and destructive, resolves one KWin UUID plus app/PID validation, uses the bridge's `Window.closeWindow()` API, and confirms that exact UUID disappeared without a keyboard fallback. Known close/quit shortcuts on retained agent seats fail closed because same-process Firefox windows demonstrate that process identity and compositor delivery acknowledgement cannot prove which window handled an application-global shortcut.
- Retained-frame invalidation is now scoped to preview-derived pointer coordinates. Direct agent-seat keyboard, scroll, and window-local input continue after the target-local quiet period without requiring an unrelated screenshot refresh, while capture-output points still fail closed until a fresh matching revision is acquired. Firefox page tabs exposing one unlabeled AT-SPI action are normalized to `select`, matching Firefox's live `available_actions=[";;"]` contract without guessing for other roles.
- Model-facing `observe` and polling post-action observations now consume the injected window/screen backends rather than bypassing them. Desktop observation assembly, compact post-action accessibility projection, and revision generation are isolated in `crates/seatgeistd/src/observation.rs` with mock-backed coverage.
- Pre-execution active-window guards and app-policy window/target reads now use the injected `WindowBackend` and live in `crates/seatgeistd/src/window_safety.rs`. Their original fail-closed order is preserved before rate-limit acceptance and action execution; deterministic mock tests cover matching/stale guards plus denied active and focus-target applications.
- Sticky target binding, renew/status identity checks, raw-action re-resolution, bounded focus verification, cooperative restoration, and post-action target validation now use that same `WindowBackend`. The duplicate daemon runtime focus handle was removed, backend provenance is journaled through `backend_name`, and the testkit mock supports active-window transitions for deterministic verification polling.
- The nine guarded semantic actions now list correlation candidates through the injected `WindowBackend` before enforcing KWin/AT-SPI PID, title, application, target-window, and app-policy identity. Async authorization lives in `crates/seatgeistd/src/target.rs`; unguarded semantic actions remain explicitly uncorrelated and skip the lookup.
- Version-2 live background-semantic evaluations now pass for both disposable Firefox and KCalc targets while a non-target user window remains active. The evaluator accepts physical switches among non-target work windows, still fails if the semantic target becomes active, makes no focus/raw/screenshot call, and verifies the exact guarded action journal. These private artifacts prove the slice live; they will be regenerated on the final Step 12 workspace fingerprint before aggregate acceptance.
- `make smoke-focus` validates the focus path in a host KDE session with a private approval file, grants only `focus_window`, and confirms the focus request is journaled.
- `seatgeist-mcp --stdio` now implements the first MCP server slice: `initialize`, `ping`, `tools/list`, and `tools/call` for current daemon-backed observe/window/screenshot/focus/journal tools.
- `seatgeist-cli observe` and MCP `seatgeist.observe` return monitors, windows, active-window state when available, and optional screenshot metadata for a bounded preview capture.
- `make smoke-mcp` validates MCP initialize, tool discovery, daemon-backed `seatgeist.health`, daemon-backed `seatgeist.observe`, and daemon-backed `seatgeist.a11y_text_attributes` error propagation over stdio. This deterministic MCP smoke is included in `make verify`.
- `crates/seatgeist-mcp/tests/stdio_integration.rs` starts a real private daemon, drives the real MCP stdio binary through JSON-RPC, and verifies initialize, tool discovery, daemon-backed health, daemon-backed AT-SPI text-attribute error propagation, default raw-input policy denial for `seatgeist.type_text`, MCP client attribution in the daemon journal, and MCP journal visibility without requiring a live desktop.
- The Codex plugin bundle now has finalized metadata, a validated MCP config for `seatgeist-mcp --stdio`, four action-oriented skills that name current `seatgeist.*` tools and safety rules, and a Codex `Stop` audit hook that writes compact repo/journal metadata plus failure, method, safety-class, client tool/process fallback, unguarded-control, active-window, and opt-in artifact metadata summary data to `target/seatgeist-hook-audit/latest.json` after Codex's normal `/hooks` trust review.
- `scripts/validate-plugin.py`, `make validate-plugin`, and `make verify` now validate the plugin manifest, relative paths, MCP config, skill frontmatter, required skill set, bundled Stop audit hook, and audit aggregation behavior without external Python dependencies.
- `scripts/check-local-codex-install.py` and `make check-local-codex-install` validate the operator's real Codex config and PATH after plugin install or checkout renames. The check confirms the `seatgeist-local` marketplace source points at the current checkout, the installed plugin cache validates, this checkout is trusted when configured, and `seatgeist-mcp`, `seatgeist-cli`, and `seatgeistd` resolve without stale old-checkout symlinks.
- `seatgeist-cli clipboard status`, MCP `seatgeist.clipboard_status`, and the status replay trace now expose safe clipboard backend diagnostics without reading clipboard contents. They report `wl-paste`, `wl-copy`, and KDE Klipper DBus availability plus selected read/write backends and setup hints.
- `seatgeist-cli clipboard get|set` and MCP `seatgeist.clipboard_get_text` / `seatgeist.clipboard_set_text` now use the Wayland `wl-paste` / `wl-copy` backend first and fall back to KDE Klipper DBus (`org.kde.klipper`) when the Wayland commands are unavailable. Clipboard reads remain prompt-gated by default and require either a matching approval-file grant or `seatgeistd --allow-clipboard-read`. Clipboard reads return at most 64 KiB by default, preserve UTF-8 boundaries when truncated, and include backend provenance. Clipboard journal/MCP summaries report length, truncation, byte count, and backend only.
- Production clipboard execution is now isolated in `crates/seatgeistd/src/clipboard.rs`. Both Wayland and KDE Klipper adapters implement the shared async `ClipboardBackend` trait; the module owns fail-closed selection, safe status probes, bounded UTF-8 reads, and provenance, while dispatcher policy checks still run before execution. Trait conformance, backend priority, bounds, and status hints have focused tests.
- `make smoke-clipboard` validates the clipboard status and content paths with a private approval file, grants only `clipboard_get`, restores the previous clipboard text when it can read it, and removes temporary clipboard-content files.
- `seatgeist-cli atspi tree --focused` and MCP `seatgeist.a11y_focused_tree` now return a compact AT-SPI subtree rooted at the focused accessibility node. The first backend discovers the accessibility bus via `org.a11y.Bus.GetAddress`, walks application roots, detects focus from the AT-SPI state bitset, and reports role/name/states/bounds/action names/children with depth and node caps.
- AT-SPI node summaries now include bounded text/value summaries for nodes implementing `org.a11y.atspi.Text` or `org.a11y.atspi.Value`. Text reads are capped at 512 characters and report `value_truncated`; password roles are marked sensitive and values are not read.
- `seatgeist-cli atspi find` and MCP `seatgeist.a11y_find` now find compact AT-SPI nodes by role, name substring, application name, and containing window name with result/depth/node caps.
- Focused-tree discovery and named, app-filtered, depth-zero AT-SPI searches use the application's standard cache as a bulk discovery fast path, then detail-query only the focused subtree or matching nodes and cached ancestors needed for action metadata and KWin-window correlation. Applications without a usable cache and deeper/window-name query shapes retain the bounded recursive fallback.
- `seatgeist-cli atspi invoke --node <id> --action <press|focus|select>` and MCP `seatgeist.a11y_invoke` now invoke advertised AT-SPI actions by resolving normalized actions to the node's `GetActions` index and calling `DoAction`. This is semantic control and remains denied by default until the daemon sees a matching approval-file grant or is started with an explicit control approval mode.
- `seatgeist-cli atspi set-text --node <id> <text>` and MCP `seatgeist.a11y_set_text` now replace non-sensitive `EditableText` contents through `SetTextContents`, cap replacement text at 8192 characters, and journal only text length.
- `seatgeist-cli atspi insert-text --node <id> --offset <n> <text>` and MCP `seatgeist.a11y_insert_text` now insert UTF-8 text into non-sensitive `EditableText` nodes through `InsertText(position, text, byte_length)`, cap inserted text at 8192 characters, and journal only text length plus offset.
- `seatgeist-cli atspi delete-text --node <id> --start-offset <n> --end-offset <n>` and MCP `seatgeist.a11y_delete_text` now delete non-sensitive `EditableText` character-offset ranges through `DeleteText(start, end)` without copying text to the clipboard, and journal only offsets.
- `seatgeist-cli atspi copy-text --node <id> --start-offset <n> --end-offset <n>` and MCP `seatgeist.a11y_copy_text` now copy non-sensitive `EditableText` character-offset ranges into the system clipboard through `CopyText(start, end)`, do not inspect clipboard contents, and journal only offsets.
- `seatgeist-cli atspi cut-text --node <id> --start-offset <n> --end-offset <n>` and MCP `seatgeist.a11y_cut_text` now cut non-sensitive `EditableText` character-offset ranges into the system clipboard through `CutText(start, end)`, do not inspect clipboard contents, and journal only offsets.
- `seatgeist-cli atspi paste-text --node <id> --offset <n>` and MCP `seatgeist.a11y_paste_text` now paste current system clipboard text into non-sensitive `EditableText` nodes through `PasteText(position)`, validate the paste offset, do not inspect clipboard contents, and journal only the offset.
- `seatgeist-cli atspi set-caret --node <id> --offset <n>` and MCP `seatgeist.a11y_set_caret` now move the caret on non-sensitive text nodes through `Text.SetCaretOffset(offset)`, validate the offset, and journal only the offset.
- `seatgeist-cli atspi set-selection --node <id> --selection-num <n> --start-offset <n> --end-offset <n>` and MCP `seatgeist.a11y_set_selection` now update an existing text selection on non-sensitive text nodes through `Text.SetSelection(selectionNum, start, end)`, validate the selection index and range, and journal only the selection index and offsets.
- `seatgeist-cli atspi text-attributes --node <id> --offset <n>` and MCP `seatgeist.a11y_text_attributes` now inspect non-sensitive `Text.GetAttributeRun(offset, includeDefaults)` attribute runs and journal only range/count metadata.
- `seatgeist-cli semantic click-button --name <name>` and MCP `seatgeist.click_button` now implement the first high-level semantic action. The daemon searches AT-SPI buttons with optional app/window guards, requires a single non-sensitive pressable match, refuses ambiguity with bounded candidate choices, and invokes the matched button through the same policy-gated semantic control path.
- `seatgeist-cli semantic set-text-field --name <name> <text>` and MCP `seatgeist.set_text_field` now implement high-level text-field mutation. The daemon searches non-sensitive AT-SPI text fields with optional app/window guards, requires a single viable match, refuses ambiguity, calls `EditableText` set-text, and journals only replacement length.
- `seatgeist-cli semantic focus-text-field --name <name>` and MCP `seatgeist.focus_text_field` now implement high-level text-field focus. The daemon searches non-sensitive focusable AT-SPI text fields with optional app/window guards, requires one viable focus-action match, refuses ambiguity, and invokes the advertised focus action before keyboard typing workflows.
- `seatgeist-cli semantic select-menu --path File/Open` and MCP `seatgeist.select_menu` now implement visible AT-SPI menu-path selection. The daemon requires at least one path segment, searches visible menu hierarchy nodes with optional app/window guards, requires one non-sensitive activatable item, refuses missing or ambiguous paths, and invokes select or press.
- `seatgeist-cli semantic activate-tab --name <name>` and MCP `seatgeist.activate_tab` now implement high-level tab activation. The daemon searches non-sensitive AT-SPI tab nodes with optional app/window guards, requires a single viable match, refuses ambiguity, and invokes select or press.
- `seatgeist-cli semantic activate-link --name <name>` and MCP `seatgeist.activate_link` now implement high-level link activation. The daemon searches non-sensitive AT-SPI link nodes with optional app/window guards, requires a single viable match, refuses ambiguity, and invokes press or select.
- `seatgeist-cli semantic toggle-check --name <name> [--checked true|false]` and MCP `seatgeist.toggle_check` now implement high-level checkbox/radio/checkable-menu control. The daemon searches non-sensitive AT-SPI check roles with optional app/window guards, requires a single viable match, refuses ambiguity, uses `checked` state for no-op desired-state requests, and invokes press or select only when a toggle is needed.
- `seatgeist-cli semantic set-value --name <name> --value <number>` and MCP `seatgeist.set_value` now implement high-level value control for non-sensitive AT-SPI sliders, spin buttons, scrollbars, and dials. The daemon requires one viable numeric value node, refuses ambiguity, and sets `org.a11y.atspi.Value.CurrentValue` through the policy-gated semantic control path.
- `seatgeist-cli semantic select-item --name <name>` and MCP `seatgeist.select_item` now implement high-level selection for non-sensitive AT-SPI list items, tree items, table rows, combo boxes, options, and menu-item-like choices. The daemon requires one viable selectable/pressable match, refuses ambiguity, and invokes select or press through the policy-gated semantic control path.
- Ambiguous high-level semantic refusals now include compact non-sensitive candidate choices with a 1-based choice index, deterministic candidate id that is stable across raw AT-SPI node-id churn, raw node id, role, accessible name, deterministic name-match score, and action metadata, capped at five choices plus an omitted-count marker.
- `seatgeist-cli atspi quality-status`, daemon `accessibility_quality_status`, and MCP `seatgeist.a11y_quality_status` now sample a bounded focused AT-SPI tree and report whether semantic targeting looks reliable, including node/name/action/text/generic-role counts, flat-tree detection, a fallback recommendation, and setup hints. The checked-in status replay trace covers this diagnostic without requiring a live desktop.
- Flat and mostly generic weak accessibility-tree fixtures now exercise the same quality-status response and compact daemon summary used by operator diagnostics.
- MCP stdio integration now verifies configured denial kinds for focus guard, human-input pause, app policy denial, forced missing portal, and invalid AT-SPI node cases. These scenarios run against private daemons and assert structured `data.kind` plus compact error text without sending desktop input or opening portal consent UI.
- `make smoke-atspi` validates the daemon-backed focused accessibility tree, accessibility find paths, text-attribute request validation, and deny-by-default accessibility invoke/set-text/insert-text/delete-text/copy-text/cut-text/paste-text/set-caret/set-selection/click-button/set-text-field/focus-text-field/select-menu/activate-tab/activate-link/toggle-check/set-value/select-item paths, and confirms the requests are journaled.
- `crates/seatgeistd/tests/protocol_integration.rs` now starts a real daemon binary on a private Unix socket and verifies health, policy, capabilities, and journal-tail responses over the newline JSON protocol.
- `crates/seatgeist-cli/tests/daemon_cli_integration.rs` now starts a real daemon binary and verifies that the actual CLI binary can call `doctor`, `capabilities`, `policy-status`, and `journal tail` over a private Unix socket.
- `seatgeist-testkit` now provides deterministic mock implementations of the screen, window, input, clipboard, and accessibility backend traits, including call recording for future unit tests that must not touch the real desktop. The screen mock covers raw and scaled screenshots; the input mock covers move, click point/button/count, drag, scroll, text, and key-combo events; the accessibility mock covers focused-tree reads, find requests, invoke calls, set-text calls, insert-text calls, delete-text calls, copy-text calls, cut-text calls, paste-text calls, caret-set calls, selection-set calls, and numeric set-value calls.
- `ScreenshotTransform::output_to_source_point` maps downscaled screenshot output coordinates back to source coordinates, with protocol tests for 8K preview and physical-pixel tile calibration.
- `scripts/gui-eval.sh` and `make gui-eval` run local evals against a private per-invocation daemon socket for current non-control flows: status, safety/session preflight metadata, observe, accessibility-quality diagnostics, focused AT-SPI tree sampling when available, AT-SPI find when available, AT-SPI text-attribute inspection when a text node is available, direct AT-SPI, semantic, and raw-input deny-by-default policy checks, clipboard backend status without reading contents, default clipboard-read denial, KWin bridge status metadata, EIS keymap status metadata, bounded screenshot preview metadata, screenshot preview coordinate mapping, configured screenshot preview/tile safety bounds, full-resolution screenshot policy denial, and journal output.
- Focused safe wrappers included in `make verify` are `make gui-eval-status`, `make gui-eval-session-preflight`, `make gui-eval-observe`, `make gui-eval-a11y-quality-status`, `make gui-eval-a11y-focused-tree`, `make gui-eval-a11y-find`, `make gui-eval-a11y-text-attributes`, `make gui-eval-a11y-control-denied`, `make gui-eval-semantic-denied`, `make gui-eval-input-denied`, `make gui-eval-clipboard-status`, `make gui-eval-clipboard-denied`, `make gui-eval-full-resolution-denied`, `make gui-eval-kwin-bridge-status`, `make gui-eval-keymap-status`, and `make gui-eval-control-safety`. Opt-in screenshot and local-input wrappers stay outside `make verify` because they may show consent dialogs, capture pixels, or send real input. Each invocation writes a unique artifact directory under `target/seatgeist-gui-eval/` and updates `target/seatgeist-gui-eval/latest`; parallel evals no longer remove each other's sockets or artifacts, and KWin-bridge DBus evals take a local lock before starting their private daemon because the bridge service name is session-global. On failure, the script reports the artifact directory, daemon log tail, journal tail, and artifact file list before daemon cleanup.
- `scripts/gui-eval.sh status` and `make gui-eval-status` validate daemon health, capabilities, policy status, and journal evidence without touching desktop backends. This safe status eval is included in `make verify`.
- `scripts/gui-eval.sh observe` and `make gui-eval-observe` validate the daemon-backed observation shape plus journal evidence without requesting screenshots, portal prompts, clipboard reads, or input. This safe observe eval is included in `make verify`.
- `scripts/gui-eval.sh a11y-quality-status` and `make gui-eval-a11y-quality-status` validate accessibility-quality diagnostic shape plus journal evidence without invoking accessibility control, screenshots, portal prompts, clipboard reads, or input. This safe diagnostic eval is included in `make verify`.
- `scripts/gui-eval.sh a11y-focused-tree` and `make gui-eval-a11y-focused-tree` validate the focused AT-SPI tree shape plus journal evidence when a focused tree is available, and skip cleanly when the quality diagnostic reports AT-SPI or a focused node unavailable. This read-only eval is included in `make verify`.
- `scripts/gui-eval.sh a11y-find` and `make gui-eval-a11y-find` validate bounded AT-SPI application-role search shape plus journal evidence when AT-SPI is available, and skip cleanly when the quality diagnostic reports AT-SPI unavailable or the find call returns a journaled `AccessibilityUnavailable` runtime error. This read-only eval is included in `make verify`.
- `scripts/gui-eval.sh a11y-text-attributes` and `make gui-eval-a11y-text-attributes` validate text-role search plus `accessibility_text_attributes` response shape and journal evidence when a text node is available, and skip cleanly when AT-SPI, AT-SPI find, text nodes, or text-attribute reads are unavailable. This read-only eval is included in `make verify`.
- `scripts/gui-eval.sh a11y-control-denied` and `make gui-eval-a11y-control-denied` validate direct AT-SPI invoke/edit/caret/selection control methods fail closed under default policy and are journaled as `ControlSemantic` `PolicyPromptRequired` denials before AT-SPI backend control can run. This safe denial eval is included in `make verify`.
- `scripts/gui-eval.sh semantic-denied` and `make gui-eval-semantic-denied` validate that all high-level semantic control actions fail closed under default policy and are journaled as `ControlSemantic` `PolicyPromptRequired` denials before AT-SPI backend control can run. This safe denial eval is included in `make verify`.
- `scripts/gui-eval.sh input-denied` and `make gui-eval-input-denied` validate raw keyboard and pointer controls fail closed under default policy and are journaled as `ControlKeyboard` or `ControlPointer` `PolicyPromptRequired` denials before uinput/EIS backend control can run. This safe denial eval is included in `make verify`.
- The safe clipboard GUI evals are now part of `make verify`: `make gui-eval-clipboard-status` validates backend diagnostics and journaling without reading clipboard contents, and `make gui-eval-clipboard-denied` validates fail-closed clipboard-read policy plus denial journaling without clipboard-read approval.
- `scripts/gui-eval.sh full-resolution-denied` and `make gui-eval-full-resolution-denied` validate that explicit full-resolution screenshot requests fail closed before writing output and are journaled as denied `FullResolutionScreenshot` requests. This safe denial eval is included in `make verify`.
- `scripts/gui-eval.sh session-preflight` and `make gui-eval-session-preflight` validate compact `safety_status`, including the default-disabled journal artifact metadata flag, sanitized `desktop_session_status` metadata, and aggregated `computer_use_readiness` metadata plus journal evidence without invoking KDE control, portal prompts, screenshots, clipboard reads, or input. This safe preflight eval is included in `make verify`.
- `scripts/gui-eval.sh kwin-bridge-status` and `make gui-eval-kwin-bridge-status` validate compact `kwin_bridge_status` metadata, including active-window and window-list update state, plus journaling without installing the KWin script or sending input. When the private daemon's DBus receiver is reachable through `qdbus6`, the eval seeds both `UpdateActiveWindow` and `UpdateWindows` and requires the status response to report the seeded active window, window-list update state, and seeded window count. This safe DBus status eval is included in `make verify`.
- `scripts/gui-eval.sh keymap-status` and `make gui-eval-keymap-status` validate compact `input_backend_status.eis_keymap` source/setup metadata without sending input. When KDE current-layout DBus or `kxkbrc` layout data is visible, the eval checks that the reported keymap source/layout metadata follows the live KDE evidence, and it verifies `input_backend_status` journaling. This safe keymap metadata eval is included in `make verify`.
- `scripts/gui-eval.sh portal-screenshot` and `make gui-eval-portal-screenshot` now run an explicit opt-in live portal Screenshot eval. It skips when the Screenshot interface is not visible; otherwise it verifies `capture-backends` selects `portal_screenshot`, captures a bounded screenshot and a physical-pixel screenshot tile through the private daemon when the portal returns screenshots, checks output metadata and image existence, and confirms journal provenance for both `screenshot` and `screenshot_tile`. Portal cancellation is treated as a skip unless `SEATGEIST_PORTAL_SCREENSHOT_STRICT=1` is set. This target is not part of `make verify` because xdg-desktop-portal may show a consent dialog.
- `scripts/portal-screenshot-v3-status.py` and `make portal-screenshot-v3-status` provide a read-only Arch/KDE portal-target diagnostic. It reports the user-session Screenshot interface version, `AvailableTargets` bitmask, installed portal/KDE package versions, local pacman pending upgrades, and `aur-step` presence without installing packages, restarting services, opening portal consent, or capturing pixels. Use it before treating `--portal-target` failures as Seatgeist bugs.
- `scripts/gui-eval.sh journal-artifacts` and `make gui-eval-journal-artifacts` now run an explicit opt-in screenshot artifact journal eval. It starts a private daemon with `[journal].include_artifact_metadata = true`, verifies `safety_status` reports the opt-in flag, captures one bounded screenshot when a screenshot backend is available, and verifies the journal artifact path, byte count, and SHA-256 against the written PNG. Portal cancellation is treated as a skip unless `SEATGEIST_PORTAL_SCREENSHOT_STRICT=1` is set. This target is not part of `make verify` because screenshot capture may show a consent dialog.
- `scripts/gui-eval.sh remote-desktop-probe` and `make gui-eval-remote-desktop-probe` run an explicit opt-in live RemoteDesktop consent-path eval. It skips when the RemoteDesktop portal interface or active-window guard metadata is unavailable; otherwise it grants only `remote_desktop_session_probe`, requests keyboard and pointer devices through the private daemon, checks compact response metadata and journaling, and requires a started session only when `SEATGEIST_REMOTE_DESKTOP_STRICT=1` is set. This target is not part of `make verify` because xdg-desktop-portal may show a consent dialog.
- `scripts/gui-eval.sh remote-desktop-eis-session` and `make gui-eval-remote-desktop-eis-session` run an explicit opt-in live retained EIS eval. It skips when the RemoteDesktop portal interface or active-window guard metadata is unavailable, and accepts a cancelled/ended portal session unless `SEATGEIST_REMOTE_DESKTOP_EIS_STRICT=1` is set. When a stored EIS session becomes active, it checks backend status and session metadata, then attempts one minimal scroll and one minimal `Shift` key-combo only after method-scoped `scroll_pointer` and `key_combo` approvals and readiness gates pass; `SEATGEIST_REMOTE_DESKTOP_EIS_INPUT_STRICT=1` requires those input attempts to succeed. This target is not part of `make verify` because it may show a portal dialog and can send real input.
- `scripts/gui-eval.sh control-safety` and `make gui-eval-control-safety` run a private daemon with an isolated panic-stop file and method-scoped approval-file grant, seed active-window state through the daemon KWin bridge when possible, and verify wrong-window guard denial plus panic-stop denial before backend control execution. The control-safety eval is included in `make verify`.
- `scripts/gui-input-smoke.sh text-editor`, `make smoke-gui-input`, and `make gui-eval-text-editor-input` run the opt-in KWrite/Kate text-editor eval. It opens a disposable file, grants only `focus_window`, `click_pointer`, `type_text`, and `key_combo`, requires active-window guard metadata before input, maps a guarded window point to physical pixels, types a sentinel in short guarded chunks, saves it, verifies file contents, and checks journal evidence. This target is not part of `make verify` because it sends real local input.
- `scripts/gui-calculator-smoke.sh kcalc` and `make gui-eval-kcalc-visual` run the opt-in KCalc visual calculation eval. It opens KCalc, grants only `focus_window`, `type_text`, and `key_combo`, requires active-window guard metadata before input, sends `2+2=` as short guarded text chunks, verifies focus/type/key journal evidence, and writes a KCalc active-window screenshot artifact showing the result when Spectacle is available. This target is not part of `make verify` because it sends real local input.
- `scripts/gui-browser-smoke.sh firefox-localhost-button` and `make gui-eval-firefox-localhost-button` run the opt-in Firefox localhost button eval. It starts a temporary localhost server, launches Firefox with a disposable profile, grants only `focus_window`, `click_pointer`, and `key_combo`, requires active-window guard metadata before input, clicks a large localhost test button through guarded window-local pointer coordinates, verifies the page POST reached the local server, checks journal evidence, and writes a Firefox active-window screenshot artifact when Spectacle is available. This target is not part of `make verify` because it sends real local input.
- GUI eval scripts now write `evidence.json` with `type=seatgeist_eval_evidence`, the eval case, outcome status, current git hash, host name, artifact directory, and artifact file list. `make release-readiness` requires matching current-commit passed evidence records for the release-blocking opt-in live evals before public release can be marked ready.
- `SEATGEIST_RELEASE_LIVE_EVALS_APPROVED=1 make release-live-evals` is the guarded release evidence runner for the six release-blocking opt-in live evals. It refuses to run without the explicit environment variable, forces strict portal and RemoteDesktop behavior so skips do not satisfy release evidence, and checks the `live_eval_evidence` readiness item after the evals complete.
- `make check-public-name` writes `target/seatgeist-release/name-collision-check.json` with exact-name crates.io, npm, PyPI, and GitHub repository checks for the Seatgeist package names. `make release-readiness` reports that evidence when it matches the current commit. This is release evidence only, not trademark or legal clearance.
- `make release-external-preflight` prints a read-only public/operator prerequisite report for final Cargo public URLs, exact-name collision evidence, local Codex plugin install readiness, release-blocking live eval evidence, signed release tag presence, and local signed artifact readiness. It supports `--json` and `--strict` through `scripts/release-external-preflight.py`, but it does not create tags, upload files, publish packages, or run live GUI input.
- `make write-release-evidence` writes retained JSON release evidence next to the current release manifest: `<manifest-prefix>.readiness.json` from `scripts/release-readiness.py --json` and `<manifest-prefix>.portal-screenshot-v3-status.json` from the read-only portal v3 diagnostic. It then runs `scripts/verify-release-evidence.py` to confirm the snapshots are linked to the selected manifest and have the expected readiness/reporting shape. These files are release-candidate evidence snapshots; they do not mark a blocked candidate as ready.
- The repo now includes `.agents/plugins/marketplace.json` for a local Codex marketplace named `seatgeist-local`. `make smoke-codex-plugin-install` uses an isolated `CODEX_HOME` to register that marketplace, install `seatgeist@seatgeist-local`, verify the installed skills/hooks/MCP files, validate the installed plugin bundle, and check `seatgeist-mcp --help`.
- `ReplayTrace`, `seatgeist-cli trace validate --file <path>`, `seatgeist-cli trace validate --dir <path>`, `seatgeist-cli trace replay --file <path>`, and `seatgeist-cli trace replay --dir <path>` provide replayable daemon-request traces with expected response metadata, including optional error-message substring expectations and JSON-pointer equality, single-type, type-list, and existence checks for compact response-field assertions. Validation checks trace version, non-empty/unique labels, expected response types, JSON-pointer syntax, supported JSON value types, and contradictory error expectations without contacting the daemon; directory validation/replay reject empty trace sets and return compact aggregate reports. Replayed steps still flow through daemon policy enforcement and journaling, include `error_kind` for error responses, and mismatch errors include step index, label, method, and expected/actual details without echoing full response payloads. `examples/traces/status-smoke.json` is a checked-in status-only replay fixture and now covers safety status journal-artifact default metadata, AT-SPI quality diagnostics, KWin bridge diagnostics, uinput access diagnostics, capture backend diagnostics, clipboard backend diagnostics including nullable selected read/write backend fields, `input_backend_status.eis_keymap` metadata, and inactive RemoteDesktop EIS session status/stop responses, `examples/traces/journal-tail-smoke.json` verifies journal method/success filters and compact default journal records without control/artifact payload fields, `examples/traces/policy-denials-smoke.json` is a safe deny-by-default fixture for protected screenshot, clipboard-read, focus-control, and AT-SPI caret/selection control actions, `examples/traces/semantic-denials-smoke.json` verifies high-level semantic click/edit/focus/tab/link/toggle/value/item/menu controls fail closed before AT-SPI backend side effects, `examples/traces/input-denials-smoke.json` verifies raw keyboard, pointer, RemoteDesktop session probe, RemoteDesktop EIS probe, and RemoteDesktop EIS start requests fail closed before backend input side effects, and `examples/traces/panic-stop-smoke.json` verifies panic-stop state transitions through response-field assertions; all are covered by real CLI integration tests, `make validate-traces`, `make smoke-trace-replay`, and the standard `make verify` gate.
- Daemon errors now serialize as `type=error` with `data.kind` plus `data.message`. CLI and MCP compact text include the kind, and checked-in policy/semantic/input denial traces assert and report `policy_prompt_required`. A real configured-daemon CLI replay test now covers focus-guard, human-input-pause, app-policy, forced portal-unavailable, and accessibility-unavailable kinds without invoking unsafe desktop control or portal consent UI. The daemon also classifies representative weak accessibility-tree, validation, and backend failures for operator troubleshooting.
- `seatgeist-cli panic-stop status|enable|disable` now controls a daemon-side file-backed panic-stop flag. The daemon journals these requests and rejects control-class actions while the flag is active, even when control policy is explicitly allowed.
- MCP now exposes `seatgeist.panic_stop_status`, `seatgeist.panic_stop_enable`, and `seatgeist.panic_stop_disable` through the same daemon-backed panic-stop protocol.
- `scripts/seatgeist-panic-stop-hotkey` is a KDE global-shortcut friendly wrapper that enables panic-stop through `seatgeist-cli`, preserving daemon journaling and `SEATGEIST_SOCKET` overrides. `docs/arch-kde-install.md` documents installing it into `~/.local/bin` and binding it as a KDE custom shortcut, and `make validate-install-assets` syntax-checks and validates the helper.
- `seatgeist-cli journal tail` and MCP `seatgeist.journal_tail` now support method and success filters for compact troubleshooting, for example `--method focus_window --ok false`.
- Current control requests now support optional active-window guards. CLI/MCP callers can pass expected active window id, app id, or title substring; the daemon rejects the request before execution if the active window no longer matches.
- `[safety].require_focus_guard` now defaults to true, so control-class requests require an active-window guard by default unless a scoped local development config explicitly opts out.
- `[safety].control_rate_limit_per_minute` now defaults to 120 accepted control-class requests over a rolling 60-second window. Set it to 0 only for scoped local development; observe/status requests are unaffected.
- `seatgeist-cli safety-status` and MCP `seatgeist.safety_status` now expose a compact read-only safety preflight: focus-guard enforcement, human-input pause state, fresh activity-signal state, quiet interval, optional signal path, control rate limit, screenshot preview/tile max-edge defaults, screenshot redaction count, and whether opt-in journal artifact metadata is enabled.
- `make smoke-human-input-pause` starts a private daemon with `[safety].pause_on_human_input = true`, writes a fresh activity signal, verifies `safety-status` reports the fresh signal, then confirms an approved focus-control request is denied and journaled before any backend focus action can run. This deterministic safety smoke is included in `make verify`.
- `seatgeist-cli desktop-session-status` and MCP `seatgeist.desktop_session_status` now expose sanitized KDE/Wayland/session environment diagnostics for DBus, runtime-directory, KWin, portal, and AT-SPI troubleshooting without returning raw session bus or runtime paths. Daemon capabilities advertise this as `daemon_desktop_session_status`.
- `seatgeist-cli readiness` and MCP `seatgeist.computer_use_readiness` now aggregate safe preflight status without screenshots, portal sessions, clipboard reads, or input. The response reports readiness booleans for observe, screenshot, window control, keyboard, pointer, semantic actions, clipboard read/write, active safety blockers, selected backend names, issue lists, and suggested next diagnostic tools. The checked-in status replay trace, CLI integration tests, MCP stdio integration, and `make smoke-trace-replay` cover this read-only diagnostic.
- `seatgeist-cli wait-for-change` and MCP `seatgeist.wait_for_change` now poll bounded screenshots, compute normalized RGB delta against a baseline, and return compact changed/timed-out/captures/elapsed/timeout/interval/score metadata with the latest screenshot metadata. A `timed_out=true` result means the watchdog expired without a sufficient visual delta, which is distinct from a failed command or screenshot backend error.
- Screenshot and wait-for-change compact daemon/MCP summaries now include capture backend provenance, matching the structured `ScreenshotInfo.backend` field and the project provenance rule without storing screenshot payloads in the journal.
- `seatgeist-cli kwin-bridge-status` and MCP `seatgeist.kwin_bridge_status` now report daemon DBus receiver state, whether active-window and window-list bridge updates have been seen, the latest bridge window count, and user-local KWin script package/config installation state.
- `seatgeist-uinput` now provides an initial Linux uinput keyboard backend. `seatgeist-cli input type-text`, `seatgeist-cli input key-combo`, MCP `seatgeist.type_text`, and MCP `seatgeist.key_combo` send policy-gated `ControlKeyboard` requests through the daemon; panic-stop and active-window guards apply, and summaries report text length or key count only.
- `seatgeist-uinput` now provides an initial Linux uinput pointer backend. `seatgeist-cli input move-pointer`, `click-pointer`, `drag-pointer`, and `scroll-pointer`, plus MCP `seatgeist.move_pointer`, `seatgeist.click_pointer`, `seatgeist.drag_pointer`, and `seatgeist.scroll_pointer`, send policy-gated `ControlPointer` requests through the daemon; panic-stop and active-window guards apply. Move/click/drag require explicit coordinate space. `physical_pixel` coordinates validate against monitor-derived physical bounds for scaled 8K layouts, global `logical_pixel` coordinates map through KWin monitor logical origins and scale factors, and guarded active-window `window_local` coordinates resolve through active-window logical geometry and monitor scale before uinput/EIS execution. Click supports one or two left/middle/right clicks, drag supports bounded press-move-release with left/middle/right buttons, and scroll supports vertical/horizontal wheel deltas.
- Retained screenshots now expose an actionable `capture_output` coordinate space. Pointer actions pair preview pixels with the owned session id and exact frame revision, then map through the retained transform after all policy gates. Exact KWin frames derive client-surface logical extents from native pixels and fractional output scale, and the agent-seat plugin targets `clientGeometry()` rather than decorated frame geometry. Stale revisions and invalid/out-of-bounds mappings return specific reason codes before input.
- Exact KWin capture sessions can coexist across owners and are bounded to four per owner; chooser-backed portal capture remains globally serialized. Agent-seat delivery confirmation and `settle_condition=none` no longer synchronously sample AT-SPI, while requested accessibility observation uses hard per-operation deadlines.
- Native agent input now uses up to four owner-bound virtual Wayland seats. A
  target window has one exclusive agent owner, while the physical user always
  retains priority. `kwin_input_spy_v2` cancels queued same-window input,
  applies a 350 ms target-local quiet check, and reports in-flight collisions
  as `confirmation=user_preempted`; activity in unrelated windows does not
  pause the lane. Legacy v1 activity remains daemon-compatible but cannot
  provide target-local preemption.
- Pointer coordinate resolution is isolated in `crates/seatgeistd/src/pointer_coordinates.rs`. Raw-action execution accepts an async action stage so sticky window-local mapping occurs only after focus verification; monitor and active-window reads use injected screen/window backends. One action context resolves both drag endpoints against one snapshot, and deterministic tests assert that multiple window-local points cause exactly one active-window read.
- Daemon configuration is isolated in `crates/seatgeistd/src/config.rs`. The module owns the TOML schema, CLI/file path precedence, backend preference, policy and app normalization, journal options, safety defaults and bounds, and redaction regions; the daemon dispatcher retains only resolved runtime settings. Existing config and protocol integration tests cover the extracted boundary.
- `seatgeist-cli input uinput-status`, MCP `seatgeist.uinput_status`, and `make smoke-uinput-status` now provide safe uinput access diagnostics without sending input. The status reports `/dev/uinput` availability, character-device state, mode/owner ids, daemon effective uid/gid, and a setup hint. `docs/uinput-setup.md` documents the optional udev rule, user service setup, current polkit placeholder state, and safe smoke flow. `make validate-install-assets` validates the shipped systemd user service, socket privacy, narrow udev rule, and current polkit placeholder defaults without mutating the host. The safe uinput/input-backend status smoke is included in `make verify`.
- `seatgeist-cli input status` (`input backends` is an alias), MCP `seatgeist.input_backend_status`, and `make smoke-uinput-status` now provide safe portal/libei/uinput backend probes without starting a RemoteDesktop session or sending input. The probe reports xdg-desktop-portal RemoteDesktop interface visibility, KDE portal service visibility, libei client metadata/socket hints, uinput fallback availability, the configured backend request, the preferred visible backend, the currently implemented backend, and compact EIS keymap source/layout metadata. `[backends].input`, `--input-backend`, and `SEATGEIST_INPUT_BACKEND` can request `auto`, `uinput`, `portal_remote_desktop`, or `libei`; raw keyboard/pointer commands route through a daemon input-executor trait. `auto` and `uinput` use the uinput executor today. Explicit portal/libei selections build EIS action plans for text, named evdev key combos, and pointer requests, then execute through the stored daemon EIS session only after the per-plan readiness gate passes; without a stored session or ready selected device they fail closed before side effects.
- Safe input backend probing is isolated in `crates/seatgeistd/src/input_diagnostics.rs`. It owns uinput device/access metadata, RemoteDesktop portal and libei visibility, configured/preferred/implemented selection, and compact setup hints while receiving only the retained-session active bit and resolved XKB status. Three focused tests cover access hints, backend priority, explicit stored-session selection, and missing-path guidance without opening a portal session or sending input.
- RemoteDesktop lifecycle code is isolated from the daemon dispatcher in two bounded modules: `portal_eis_probe.rs` owns transient session/EIS probes plus request validation, while `portal_eis_session.rs` owns retained session metadata, storage, lifecycle status, and readiness-gated execution. This extraction preserves the existing policy-gated protocol and fail-closed stored-session behavior; it adds no input path and performs no live portal or desktop interaction during verification.
- Raw input backend selection and execution are isolated in `crates/seatgeistd/src/input_execution.rs`. Its shared trait, uinput adapter, retained-session EIS adapter, and common EIS plan executor sit behind the dispatcher’s existing policy, panic-stop, activity, focus, and journal gates. EIS key-combo parsing is separately isolated in `crates/seatgeistd/src/eis_key_combo.rs`, preserving named evdev parsing before bounded XKB symbol fallback. Focused routing, readiness, execution, and keymap tests continue to pass without sending live input.
- Raw keyboard and pointer action orchestration is isolated in `crates/seatgeistd/src/input_actions.rs`. The bounded module owns per-request validation, coordinate preparation, backend invocation, and compact success metadata for type, combo, move, click, drag, and scroll. Authorization, sticky focus, retained-session ownership, human-input pause, rate limiting, and journaling remain in the dispatcher, preserving their ordering around every action. Two module-local tests prove invalid keyboard and pointer bounds fail before coordinate or backend access.
- Daemon-side XKB/RMLVO configuration and KDE layout resolution are isolated in `crates/seatgeistd/src/keymap.rs`. The module separates live KDE DBus/`kxkbrc` collection from a pure, tested source resolver covering explicit configuration, current-layout priority, configured-layout fallback, malformed current metadata, and xkbcommon defaults; the dispatcher now holds only the resulting config/settings boundary.
- `seatgeist-portal` now codifies the official xdg-desktop-portal RemoteDesktop request/session/EIS contract as a tested crate boundary: service/object/interface constants, keyboard/pointer/touchscreen device bitmasks, persist modes, `CreateSession`, `SelectDevices`, `Start`, and `ConnectToEIS` bus-call builders, Request and Session handle-token/path validation, session-handle response parsing, start-response device/clipboard/restore-token parsing, a mockable lifecycle/EIS transport, a zbus lifecycle that can drive `CreateSession -> SelectDevices -> Start` while pre-subscribing to expected Request responses, and same-connection zbus `ConnectToEIS` helpers that return an owned FD. The daemon can wrap a portal-returned EIS FD in transient probe runtimes and retained daemon-owned EIS sessions; explicit portal/libei raw input uses the retained session only after policy, active-window, panic-stop, and readiness gates pass.
- `seatgeist-cli input remote-desktop-probe` and MCP `seatgeist.remote_desktop_session_probe` now provide an explicit, policy-gated transient RemoteDesktop lifecycle probe. It can request keyboard/pointer/touchscreen devices, optional restore token and persist mode, and optional active-window guard; it may show a portal consent dialog, reports selected devices/clipboard/restore metadata and request/session handles, then closes the transient session without calling `ConnectToEIS` or sending Notify*/EIS input.
- `seatgeist-cli input remote-desktop-eis-probe` and MCP `seatgeist.remote_desktop_eis_probe` now provide an explicit, policy-gated transient RemoteDesktop EIS probe. It requests the same consented session, calls `ConnectToEIS` after `Start` on the same DBus connection, initializes a transient daemon EIS runtime from the returned FD, polls pending events, closes the runtime, reports only compact metadata plus runtime connected/event/bound-capability/resumed-device counts, and sends no EIS or Notify* input.
- `seatgeist-cli input remote-desktop-eis-start`, `seatgeist-cli input remote-desktop-eis-session-status`, `seatgeist-cli input remote-desktop-eis-stop`, MCP `seatgeist.remote_desktop_eis_start`, MCP `seatgeist.remote_desktop_eis_session_status`, and MCP `seatgeist.remote_desktop_eis_stop` now expose the daemon-owned RemoteDesktop EIS session lifecycle. Start is policy-gated control and may open a portal dialog; status and stop are safe lifecycle diagnostics. These commands retain or drop the session and report compact runtime/device readiness metadata. Raw input is sent only by separate policy-gated control requests configured for portal/libei, and only after the stored-session readiness gate passes.
- Raw keyboard and pointer command handlers now use a daemon `InputExecutionBackend` trait boundary. `auto` and `uinput` use the uinput adapter; portal RemoteDesktop and libei preferences route EIS plans through the stored daemon EIS session and still fail closed before side effects when no stored session or ready selected device exists. Successful action summaries include backend provenance such as `backend=uinput` or `backend=portal_remote_desktop`.
- `seatgeist-eis` now models libei sender event plans used by stored-session EIS input execution. It covers UTF-8 text and XKB-compatible text keysyms through the text capability, named evdev key combos through the keyboard capability, absolute pointer movement, click, drag, and discrete scroll; each plan includes start/stop emulation boundaries, frames, required capabilities, Linux input-event-codes key/button values, XKB keysym conversion through xkbcommon where applicable, and 120-unit scroll steps. It also exposes an `EisEventSink` abstraction and a `LibeiDeviceSink` FFI adapter that calls libei sender APIs for a caller-owned resumed device while rejecting interior-NUL text before crossing FFI. Tested device selection now requires a resumed device with every plan capability and, for absolute pointer plans, a virtual-device region covering all target coordinates; paused devices, missing capabilities, out-of-region targets, cross-region drags, and physical absolute devices fail closed. `LibeiSenderContext` can take ownership of a portal-returned EIS FD, configure the libei sender name, expose the libei event FD for polling, dispatch pending events, bind the intersection of plan-required and seat-available capabilities on `SeatAdded`, snapshot connect/seat/device/resume/pause/remove events into compact device metadata, retain refcounted resumed device handles until pause/remove/seat removal/disconnect, and is marked `Send` for daemon mutex-serialized ownership transfer. `EisRuntimeState` consumes those snapshots, tracks connection/seat/bound-capability state plus the current device list, applies pause/remove/disconnect events, selects a resumed device for a plan from live-style state, and now reports stricter execution readiness only when the session is connected, all plan-required capabilities are bound, and a resumed matching device is selected. `EisSessionRuntime` wraps an event source plus runtime state so a daemon-owned EIS session can poll pending events, poll plan-aware seat bindings, report planning or execution readiness, and hand a ready plan to a selected-device executor only after readiness passes. The live `LibeiSenderContext` selected-device executor applies validated plans only to retained selected devices and fails closed if the selected device is no longer retained. This selected-device executor boundary is covered by mock tests and is now used by explicit portal/libei raw input when a stored EIS session is active and ready. The daemon now has a `DaemonPortalEisSession` wrapper that preserves portal session metadata while owning an EIS runtime, a mutex-backed single-session store with start/status/stop daemon protocol, plus a tested session-backed input executor that turns text/keyboard/pointer requests into EIS plans and calls the ready selected-device executor only after readiness passes. The transient EIS probe initializes a session wrapper, polls, drops it, and still sends no input. Explicit portal/libei raw-input selections now use the stored daemon EIS session after policy, panic-stop, active-window guard, and per-plan readiness checks. CLI/MCP wrappers expose stored-session start, status, and stop. The EIS crate now also owns a tested xkbcommon keymap wrapper that builds explicit RMLVO keymaps, maps level-0 keysyms to XKB keycodes, and converts those to evdev keycodes with the documented 8-code offset; explicit EIS key combos now preserve named evdev parsing first and then use configured `[backends.keymap]` RMLVO names, KDE current-layout DBus metadata, KDE `kxkbrc` config via `kreadconfig6`, or xkbcommon defaults for unsupported single-character symbol parts.
- `seatgeist-cli capture-backends`, MCP `seatgeist.capture_backend_status`, and `make smoke-capture-backends` now provide safe capture backend probes without starting a portal session or capturing pixels. The probe reports xdg-desktop-portal Screenshot/ScreenCast interface visibility, KWin supportInformation metadata availability, Spectacle fallback availability, the preferred visible capture backend, and the currently implemented capture backend. When portal Screenshot is visible, full-screen screenshot execution uses the portal through the tested `seatgeist-portal` zbus transport; tile capture now uses the same portal source before cropping/downscaling the requested physical-pixel region. Spectacle remains the compatibility fallback when portal Screenshot is unavailable or fails before a user response. The safe capture-backend smoke accepts either `portal_screenshot` or `spectacle` as the implemented backend and is included in `make verify`.
- Exact requested-window sessions now use KWin ScreenShot2 rather than portal ScreenCast. The daemon policy-checks the KWin UUID/app/PID, KWin resolves the UUID again, and bounded `snapshot`/`wait` calls recapture only that compositor window with `backend=kwin_screenshot2_window`, no chooser, no PipeWire stream, and no occlusion risk. KDE authorization is narrowly installed through a user-local desktop entry naming the daemon executable and only `org.kde.KWin.ScreenShot2`; missing authorization fails closed without silently opening a portal. Generic window requests without an exact UUID, monitor capture, and virtual-output capture retain the chooser-authoritative portal ScreenCast path and its private restore-token handling.
- Retained capture lifecycle checks now consume the pre-subscribed portal `Session.Closed` signal, reap ended sessions, expose only `last_end_reason`, free the capture slot, and reject stale-session snapshots. Sticky raw input checks the capture lifecycle before focus and immediately before injection, while capture status revalidates the pinned KWin id/app/PID so a closed target cannot silently rebind to a replacement. Opt-in revocation and close/reopen runners have deterministic fake-CLI coverage; live KDE evidence remains paused.
- Native PipeWire delivery now uses a one-frame latest-value mailbox instead of retaining the oldest unread frames in a bounded FIFO. This keeps memory bounded while collapsing intermediate compositor frames, preventing interactive snapshots from replaying stale popup or geometry states. Mailbox coalescing/disconnection tests, the full workspace suite, the headless two-output fixture, and a focused live nested-KDE rerun pass. The live rerun captured minimized Firefox, its transient application menu, a scale-correct 583x540 logical resize as 875x810 physical pixels, and movement from WL-0 to WL-1 with fresh target-only revisions. The complete same-fingerprint eight-scenario artifact is still required before Step 12 acceptance.
- The live KDE revocation probe reached inactive status with `last_end_reason=portal_closed`, cleared its sticky target, and avoided cleanup close, but exposed an evaluator mismatch: stale reuse is rejected by the centralized non-oracular owner gate as `session_owner_mismatch` after reaping clears ownership. The lifecycle runner now accepts and records that safe rejection as well as the direct `session_ended` form. Deterministic lifecycle and aggregate-contract tests pass; the live artifact will be regenerated on the final workspace fingerprint rather than rewritten.
- The cooperative sticky live runner now provisions short-lived approvals for exactly `focus_window` and `key_combo`; no wildcard grant is used. Its first fully authorized action reached KWin focus, but WindowsRunner returned success without making the pinned Firefox target active. The 750 ms bridge confirmation failed and Seatgeist returned `FocusLeaseConflict` before keyboard injection. Direct WindowsRunner probes for Firefox, KCalc, and Firestorm also remained on Konsole. A bounded native KWin focus-plugin experiment did not keep the target active and was removed from both the source and installed plugin directory. Same-seat raw focus is therefore still pending and will not receive open-ended retries; separate-seat/session designs move into Step 13 research while background AT-SPI actions remain the proven non-focus option.
- `scripts/nested-remote-desktop-probe.py` and `make probe-nested-remote-desktop` provide the first Step 13 multi-use gate. Inside the private headless KWin fixture they verify private session-bus and Wayland namespaces, KDE RemoteDesktop v2, keyboard/pointer/touch device mask `7`, and the complete lifecycle plus `ConnectToEIS` method set. The probe creates no portal session, opens no chooser, sends no input, and records private structured evidence. This selects the nested compositor as the first implementation candidate without yet claiming live input isolation; an operator-approved nested EIS action with host-focus invariants is the next gate.
- The first visible nested EIS isolation gate passed on 2026-07-11. The initial run exposed that `request_remote_desktop_eis_zbus` dropped the D-Bus connection whose unique name owned the portal session; KDE therefore closed EIS immediately. `PortalRemoteDesktopEisSession` now retains that connection for the full daemon session lifetime, while startup polls a bounded three seconds, binds the requested keyboard/pointer capability family on `SeatAdded`, and refuses to retain a session without a connected resumed device. The passing run bound keyboard, absolute-pointer, button, and scroll capabilities, exposed one resumed device, journaled one F12 through `portal_remote_desktop`, and caused only the private nested Konsole marker to change. The host active window remained the exact Codex Konsole before and after. Option C is now selected for the first persistent multi-use lane.
- Retained capture and sticky interaction sessions now record a trusted Unix-peer owner. MCP lifecycles are process-scoped, verified CLI lifecycles are tool-scoped, and a centralized pre-execution gate protects renew/snapshot/wait/close, sticky raw input, and post-action images. Status exposes compact owner tool/PID/scope metadata; non-owners receive `session_owner_mismatch` before capture, focus, settle, or input side effects. Deterministic cross-process and non-oracular-denial tests pass; no live desktop action is required for this boundary.
- Retained session status now merges a dedicated sanitized execution record containing capture/semantic/raw backend selection, target and successful-action policy results, action method/id/safety class, cooperative-focus policy, trusted activity provenance, focus-lease outcome, and final settle metadata. Only successful control actions update the last-action fields; status/snapshot/wait/renew do not. Store, dispatcher, protocol, privacy, and compact MCP tests pass without exercising the live desktop.
- Screenshot v3 target and visible-window crop compatibility are now modular one-shot `ScreenBackend` adapters. V3 target failures cannot silently fall back to full-desktop Spectacle. Expert CLI/MCP callers may explicitly request `visible_window_crop_id`; the daemon resolves exact KWin geometry, accepts only one unrotated monitor with correlated physical bounds, returns bounded window-local output with `backend=visible_window_crop`, and exposes occlusion risk. Deterministic routing/crop/privacy tests pass; live KDE behavior remains paused.
- Backend-independent screenshot image processing is isolated in `crates/seatgeistd/src/screenshot_image.rs`. Portal, Spectacle, tile, and visible-window paths share safe PNG output preparation, bounded scaling/cropping, transform-aware redaction, and dimension validation. Seven module tests cover preview/tile sizing, bounds overflow, output symlink refusal, and redaction mapping/pixels without capturing the live desktop.
- One-shot screenshot orchestration is isolated in `crates/seatgeistd/src/screenshot.rs`. It owns source exclusivity, v3 target validation, portal/Spectacle routing, tile source cleanup, screenshot metadata, and wait-for-change sampling while dispatcher policy and the image-processing module retain separate boundaries. Four orchestration tests moved with the implementation, and wait intervals now use asynchronous Tokio sleep instead of blocking a daemon runtime thread.
- Safe capture capability probing is isolated in `crates/seatgeistd/src/capture_diagnostics.rs`, covering portal Screenshot/ScreenCast introspection, v3 property and target-mask decoding, KWin/Spectacle availability, backend preference, and setup hints without capturing pixels. Shared executable/status/stdout probing now lives in `crates/seatgeistd/src/commands.rs`; screenshot, clipboard, readiness, and diagnostics no longer carry separate PATH-search implementations. Five focused tests cover the shared command boundary and capture diagnostic decisions.
- CLI JSON output now uses one fallible stdout boundary and treats `EPIPE` as normal pipeline termination. Commands such as `journal tail | grep -q` no longer panic when the reader exits after an early match; direct writer tests and the existing smoke pipelines cover the behavior.
- MCP generic resource and resource-template discovery now returns valid empty collections instead of method-not-found errors. Seatgeist still publishes no MCP resources; this removes startup noise without adding tools or daemon calls.
- The non-live audit added an explicit identity-validated `window_session renew`, moved production retained-window opening behind an injected `ScreenBackend` adapter, aborts raw injection when trusted human activity appears during focus acquisition, and supports an opt-in matching retained frame in the same settled action result. Portal cancellation is now `consent_cancelled` and excluded from reliability defects. Workspace tests, clippy, smoke, plugin/release validators, and deployment checks pass; the live daemon is updated with capture and EIS sessions idle, while KDE acceptance remains deliberately paused.
- Step 12 now has a modular offline acceptance bundle gate. Every live evidence producer records a content-addressed Git/worktree fingerprint, and `make verify-cooperative-use-acceptance` requires the exact eight owner-private artifacts, their producer types/versions and core live outcomes, one identical workspace revision, passing cooperative budgets, and bounded age/span. It rejects missing, failed, stale, malformed, or cross-revision sets and writes only compact path-free hashes/metadata. Deterministic producer-contract and CLI/Make tests pass; this does not replace the still-paused live KDE runs.
- Daemon safety runtime state is now separated into `crates/seatgeistd/src/safety_runtime.rs`, which owns panic-stop persistence, the rolling control-rate limiter, and private approval-file parsing while dispatcher policy decisions remain in `main.rs`. The shipped user service now launches the same `~/.local/bin/seatgeistd` release launcher used by the plugin/development install path instead of a potentially stale independent `~/.cargo/bin` copy; install validation enforces the path, and the local running process was rebuilt, restarted idle, and hash-matched to `target/release/seatgeistd`.
- `scripts/deploy-seatgeistd-user.py` and `make deploy-user-daemon` now encode the safe user-daemon update workflow. The helper builds the CLI and release daemon, checks retained capture/EIS idleness before installation and immediately before restart, atomically installs `~/.local/bin/seatgeistd`, waits for a successful daemon request and then the first KWin bridge heartbeat, verifies release/installed/running SHA-256 equality, and checks idleness again. Deterministic fake-command tests cover delayed readiness, delayed bridge publication, active-session refusal at both gates, atomic mode/content installation, systemd calls, and running-image hash mismatch without touching the live service.
- The daemon-side KWin bridge is now separated into `crates/seatgeistd/src/kwin_bridge.rs`, which owns the session-bus receiver, compact JSON payload validation, active-window/window-list synchronized state, trusted activity forwarding, user-local package/config discovery, enabled-state parsing, and status assembly. Shared XDG home resolution is isolated in `crates/seatgeistd/src/xdg.rs`; the daemon dispatcher consumes only clonable state handles and the module status function. Payload, merge, status, config parsing, and focus-confirmation tests continue to cover the boundary.
- `seatgeist-portal` now codifies the official xdg-desktop-portal Screenshot request contract as a tested crate boundary: service/object/interface constants, handle-token validation, expected and returned Request object paths, Response signal match rules, response-code handling, screenshot URI extraction, file URI decoding, a transport trait that drives the request lifecycle, and a zbus execution path used by the daemon for full-screen portal screenshots.
- `seatgeist-cli input pointer-calibration`, MCP `seatgeist.pointer_calibration`, and `make smoke-pointer-calibration` now provide safe pointer calibration diagnostics without moving the pointer. The response reports physical-pixel bounds derived through the injected screen backend, per-monitor physical origins, and representative top-left/center/bottom-right sample points for guarded GUI smoke. This safe calibration smoke is included in `make verify`.
- `docs/arch-kde-install.md` now provides the consolidated Arch Linux + KDE Plasma 6 operator install runbook, including package prerequisites, binary installation, user service setup, KWin bridge enablement, safe diagnostics, optional uinput, Codex plugin validation, approval flow, and troubleshooting.
- `.github/workflows/checks.yml` runs only headless formatting, workspace, plugin, install-asset, and release checks. Live desktop and portal evaluations remain explicit local operator steps.
- `LICENSE-MIT`, `LICENSE-APACHE`, `scripts/validate-release.py`, and `make validate-release` enforce that the repository has license files matching the workspace `MIT OR Apache-2.0` declaration, canonical repository metadata, a safe headless CI workflow, and the release-checklist public-readiness anchors. `make verify` runs this release validation.
- `docs/unsupported-paths.md` now documents unsupported public paths for GNOME, wlroots/Sway, X11, kernel modules, custom KDE/KWin work, screenshot/OCR fallback, native desktop approval UX, and backend-neutral packaging. `make validate-release` checks that these release-boundary anchors remain present.
- `scripts/package-release.sh` and `make package-release` now build release binaries, stage the plugin, docs, helper scripts, install assets, traces, licenses, and workspace metadata, then write a `seatgeist-<version>-<git>-linux-x86_64.tar.gz` binary/plugin archive, a tracked-file `seatgeist-<version>-<git>-source.tar.gz` source archive, a release manifest, and SHA-256 checksum files under `target/seatgeist-release/`. `scripts/verify-release-artifacts.py` and `make verify-release-artifacts` rebuild and verify the release manifest, checksums, bundle contents, and source archive contents before upload.
- `make smoke-gui-input` now validates real policy-gated uinput control in a disposable KWrite/Kate document. It starts a private daemon with an approval file, grants only the focus, click, type, and save methods it uses, focuses the test window through KWin, requires an active-window guard, maps a safe window point to physical pixels through pointer calibration, clicks, types a sentinel, saves, verifies file content, and checks the journal.
- The host GUI smoke exposed and fixed the initial keyboard mapping bug where text typing assumed evdev letter codes were contiguous; the uinput backend now uses explicit US evdev letter mappings.
- AT-SPI run-attribute mutation if a supported interface is identified, KWin-native capture backends, broader live KDE evals, and portal clipboard integration if a stable interface appears remain future work.
