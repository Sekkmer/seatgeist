# PlasmaPilot Status

## 2026-07-04

Phase 0 scaffold is present:

- Cargo virtual workspace with resolver 3.
- Rust 2024 member crates for shared types, policy, backend traits, daemon, CLI, MCP, KWin, AT-SPI, and testkit.
- Conservative plugin, MCP, hook, skill, systemd, udev, and polkit skeletons.
- The initial CLI and daemon scaffold has been replaced by the implementation slices below.

Phase 1 first slice is implemented:

- `plasma-pilotd` binds a Unix socket, enforces restrictive socket directory/socket permissions, and rejects clients from another UID using Unix peer credentials.
- The daemon serves newline-delimited JSON requests for `health`, `capabilities`, and `policy_status`.
- `plasma-pilot-cli doctor`, `capabilities`, and `policy-status` call the daemon over the Unix socket.
- `make smoke` starts a temporary daemon, calls the CLI health/capability/policy commands, and verifies socket directory/socket modes.
- The daemon and CLI can capture a full-screen PNG through Spectacle when run in the host KDE session. The smoke capture on this workstation returned a 7680x4320 source image.
- Screenshot output now defaults to a bounded preview. On the 8K workstation, the default CLI screenshot produced a 1600x900 PNG with source/output dimensions and scale metadata. Full-resolution screenshot requests are explicit and policy-gated separately; they prompt/fail closed by default until the daemon is started with full-resolution screenshot approval.
- Spectacle captures are serialized inside the daemon because concurrent Spectacle captures can race.
- `plasma-pilot-cli monitors` now reports KWin monitor metadata from `org.kde.KWin.supportInformation`; on this workstation it reports `HDMI-A-2` as 5120x2880 logical at scale 1.5, matching the 7680x4320 screenshot source.
- Screenshot responses include the same monitor metadata when KWin responds.
- `plasma-pilot-cli screenshot-tile` can crop a physical-pixel region from a full-source capture and optionally downscale the tile. It prefers the portal Screenshot source when available, falls back to Spectacle when the portal backend fails before user cancellation, and preserves backend provenance in the response. A host smoke captured a 1600x1200 tile at source origin 3200,1600 and wrote an 800x600 PNG with scale factors 0.5.
- `plasma-pilot-cli windows` lists open windows through KWin's `WindowsRunner` and enriches each stable KWin window id through `org.kde.KWin.getWindowInfo` for title, app id, logical geometry, and monitor association derived from KWin monitor overlap. The current list path still reports `pid: null` because KWin's queried list surfaces do not expose per-window pid.
- `kwin/plasma-pilot-bridge` packages the PlasmaPilot KWin script. It reads KWin's `workspace.activeWindow`, subscribes to `windowActivated`, and publishes compact active-window JSON to the daemon over the session bus.
- `plasma-pilot-cli active-window` reads the daemon's latest KWin script bridge update. Before the script reports its first update, the command still fails with the documented bridge requirement because KWin's interactive `queryWindowInfo` is not suitable for unattended active-window checks.
- `make smoke-windows` validates window listing in a host KDE session and accepts either a real active-window bridge response or the documented bridge-not-yet-reporting failure.
- `make install-kwin-script` is available as an explicit, opt-in KWin configuration mutation for installing/enabling the script.
- The KWin script was installed on this workstation and a host smoke observed a real active window with app id, pid when published by the bridge, logical geometry, and derived monitor association through the daemon bridge.
- The daemon writes compact JSONL journal entries for every handled request. Entries include method, success, safety class, guard presence, summary, and best-effort active-window before/after context for control-class requests without recording request payload text. `plasma-pilot-cli journal tail --limit N` reads recent entries through the daemon, and smoke tests verify restrictive journal file permissions.
- Daemon requests now pass through the policy engine before execution. Current observe/status requests are allowed by default; prompt-level requests fail closed unless a configured approval file contains a matching unexpired method/safety-class grant.
- `plasma-pilotd` now reads optional daemon path, approval-file path, requested raw input backend, explicit EIS XKB keymap RMLVO names, policy defaults including destructive-action and secret-field policy, app allow/deny lists, `safety.require_focus_guard`, `safety.pause_on_human_input`, and `safety.redact_regions` from `~/.config/plasma-pilot/config.toml` or `--config` / `PLASMA_PILOT_CONFIG`. CLI path arguments, `--input-backend` / `PLASMA_PILOT_INPUT_BACKEND`, and explicit approval flags remain higher precedence than file values. `plasma-pilot-cli approve` appends short-lived JSONL grants for the configured approval file; the daemon rejects insecure approval files. App deny/allow rules, required active-window guards, secret-looking high-level text fields, and fresh human-input activity signals apply before backend execution; configured screenshot redaction regions are black-filled before screenshot outputs leave the daemon.
- `plasma-pilot-cli focus --window <id>` focuses a listed KWin window through `WindowsRunner.Run`. It is a control action: default policy rejects it until a matching approval-file grant exists, while `plasma-pilotd --allow-control` remains available for explicit broad local smoke/manual use.
- `make smoke-focus` validates the focus path in a host KDE session with a private approval file, grants only `focus_window`, and confirms the focus request is journaled.
- `plasma-pilot-mcp --stdio` now implements the first MCP server slice: `initialize`, `ping`, `tools/list`, and `tools/call` for current daemon-backed observe/window/screenshot/focus/journal tools.
- `plasma-pilot-cli observe` and MCP `plasma.observe` return monitors, windows, active-window state when available, and optional screenshot metadata for a bounded preview capture.
- `make smoke-mcp` validates MCP initialize, tool discovery, daemon-backed `plasma.health`, daemon-backed `plasma.observe`, and daemon-backed `plasma.a11y_text_attributes` error propagation over stdio.
- `crates/plasma-pilot-mcp/tests/stdio_integration.rs` starts a real private daemon, drives the real MCP stdio binary through JSON-RPC, and verifies initialize, tool discovery, daemon-backed health, daemon-backed AT-SPI text-attribute error propagation, and MCP journal visibility without requiring a live desktop.
- The Codex plugin bundle now has finalized metadata, a validated MCP config for `plasma-pilot-mcp --stdio`, four action-oriented skills that name current `plasma.*` tools and safety rules, and a Codex `Stop` audit hook that writes compact repo/journal metadata plus failure, method, safety-class, unguarded-control, and active-window summary data to `target/plasma-pilot-hook-audit/latest.json` after Codex's normal `/hooks` trust review.
- `scripts/validate-plugin.py`, `make validate-plugin`, and `make verify` now validate the plugin manifest, relative paths, MCP config, skill frontmatter, required skill set, bundled Stop audit hook, and audit aggregation behavior without external Python dependencies.
- `plasma-pilot-cli clipboard get|set` and MCP `plasma.clipboard_get_text` / `plasma.clipboard_set_text` now use the Wayland `wl-paste` / `wl-copy` backend first and fall back to KDE Klipper DBus (`org.kde.klipper`) when the Wayland commands are unavailable. Clipboard reads remain prompt-gated by default and require either a matching approval-file grant or `plasma-pilotd --allow-clipboard-read`. Clipboard reads return at most 64 KiB by default, preserve UTF-8 boundaries when truncated, and include backend provenance. Clipboard journal/MCP summaries report length, truncation, byte count, and backend only.
- `make smoke-clipboard` validates the clipboard path with a private approval file, grants only `clipboard_get`, restores the previous clipboard text when it can read it, and removes temporary clipboard-content files.
- `plasma-pilot-cli atspi tree --focused` and MCP `plasma.a11y_focused_tree` now return a compact AT-SPI subtree rooted at the focused accessibility node. The first backend discovers the accessibility bus via `org.a11y.Bus.GetAddress`, walks application roots, detects focus from the AT-SPI state bitset, and reports role/name/states/bounds/action names/children with depth and node caps.
- AT-SPI node summaries now include bounded text/value summaries for nodes implementing `org.a11y.atspi.Text` or `org.a11y.atspi.Value`. Text reads are capped at 512 characters and report `value_truncated`; password roles are marked sensitive and values are not read.
- `plasma-pilot-cli atspi find` and MCP `plasma.a11y_find` now find compact AT-SPI nodes by role, name substring, application name, and containing window name with result/depth/node caps.
- `plasma-pilot-cli atspi invoke --node <id> --action <press|focus|select>` and MCP `plasma.a11y_invoke` now invoke advertised AT-SPI actions by resolving normalized actions to the node's `GetActions` index and calling `DoAction`. This is semantic control and remains denied by default until the daemon sees a matching approval-file grant or is started with an explicit control approval mode.
- `plasma-pilot-cli atspi set-text --node <id> <text>` and MCP `plasma.a11y_set_text` now replace non-sensitive `EditableText` contents through `SetTextContents`, cap replacement text at 8192 characters, and journal only text length.
- `plasma-pilot-cli atspi insert-text --node <id> --offset <n> <text>` and MCP `plasma.a11y_insert_text` now insert UTF-8 text into non-sensitive `EditableText` nodes through `InsertText(position, text, byte_length)`, cap inserted text at 8192 characters, and journal only text length plus offset.
- `plasma-pilot-cli atspi delete-text --node <id> --start-offset <n> --end-offset <n>` and MCP `plasma.a11y_delete_text` now delete non-sensitive `EditableText` character-offset ranges through `DeleteText(start, end)` without copying text to the clipboard, and journal only offsets.
- `plasma-pilot-cli atspi copy-text --node <id> --start-offset <n> --end-offset <n>` and MCP `plasma.a11y_copy_text` now copy non-sensitive `EditableText` character-offset ranges into the system clipboard through `CopyText(start, end)`, do not inspect clipboard contents, and journal only offsets.
- `plasma-pilot-cli atspi cut-text --node <id> --start-offset <n> --end-offset <n>` and MCP `plasma.a11y_cut_text` now cut non-sensitive `EditableText` character-offset ranges into the system clipboard through `CutText(start, end)`, do not inspect clipboard contents, and journal only offsets.
- `plasma-pilot-cli atspi paste-text --node <id> --offset <n>` and MCP `plasma.a11y_paste_text` now paste current system clipboard text into non-sensitive `EditableText` nodes through `PasteText(position)`, validate the paste offset, do not inspect clipboard contents, and journal only the offset.
- `plasma-pilot-cli atspi set-caret --node <id> --offset <n>` and MCP `plasma.a11y_set_caret` now move the caret on non-sensitive text nodes through `Text.SetCaretOffset(offset)`, validate the offset, and journal only the offset.
- `plasma-pilot-cli atspi set-selection --node <id> --selection-num <n> --start-offset <n> --end-offset <n>` and MCP `plasma.a11y_set_selection` now update an existing text selection on non-sensitive text nodes through `Text.SetSelection(selectionNum, start, end)`, validate the selection index and range, and journal only the selection index and offsets.
- `plasma-pilot-cli atspi text-attributes --node <id> --offset <n>` and MCP `plasma.a11y_text_attributes` now inspect non-sensitive `Text.GetAttributeRun(offset, includeDefaults)` attribute runs and journal only range/count metadata.
- `plasma-pilot-cli semantic click-button --name <name>` and MCP `plasma.click_button` now implement the first high-level semantic action. The daemon searches AT-SPI buttons with optional app/window guards, requires a single non-sensitive pressable match, refuses ambiguity with bounded candidate choices, and invokes the matched button through the same policy-gated semantic control path.
- `plasma-pilot-cli semantic set-text-field --name <name> <text>` and MCP `plasma.set_text_field` now implement high-level text-field mutation. The daemon searches non-sensitive AT-SPI text fields with optional app/window guards, requires a single viable match, refuses ambiguity, calls `EditableText` set-text, and journals only replacement length.
- `plasma-pilot-cli semantic focus-text-field --name <name>` and MCP `plasma.focus_text_field` now implement high-level text-field focus. The daemon searches non-sensitive focusable AT-SPI text fields with optional app/window guards, requires one viable focus-action match, refuses ambiguity, and invokes the advertised focus action before keyboard typing workflows.
- `plasma-pilot-cli semantic select-menu --path File/Open` and MCP `plasma.select_menu` now implement visible AT-SPI menu-path selection. The daemon requires at least one path segment, searches visible menu hierarchy nodes with optional app/window guards, requires one non-sensitive activatable item, refuses missing or ambiguous paths, and invokes select or press.
- `plasma-pilot-cli semantic activate-tab --name <name>` and MCP `plasma.activate_tab` now implement high-level tab activation. The daemon searches non-sensitive AT-SPI tab nodes with optional app/window guards, requires a single viable match, refuses ambiguity, and invokes select or press.
- `plasma-pilot-cli semantic activate-link --name <name>` and MCP `plasma.activate_link` now implement high-level link activation. The daemon searches non-sensitive AT-SPI link nodes with optional app/window guards, requires a single viable match, refuses ambiguity, and invokes press or select.
- `plasma-pilot-cli semantic toggle-check --name <name> [--checked true|false]` and MCP `plasma.toggle_check` now implement high-level checkbox/radio/checkable-menu control. The daemon searches non-sensitive AT-SPI check roles with optional app/window guards, requires a single viable match, refuses ambiguity, uses `checked` state for no-op desired-state requests, and invokes press or select only when a toggle is needed.
- `plasma-pilot-cli semantic set-value --name <name> --value <number>` and MCP `plasma.set_value` now implement high-level value control for non-sensitive AT-SPI sliders, spin buttons, scrollbars, and dials. The daemon requires one viable numeric value node, refuses ambiguity, and sets `org.a11y.atspi.Value.CurrentValue` through the policy-gated semantic control path.
- `plasma-pilot-cli semantic select-item --name <name>` and MCP `plasma.select_item` now implement high-level selection for non-sensitive AT-SPI list items, tree items, table rows, combo boxes, options, and menu-item-like choices. The daemon requires one viable selectable/pressable match, refuses ambiguity, and invokes select or press through the policy-gated semantic control path.
- Ambiguous high-level semantic refusals now include compact non-sensitive candidate choices with node id, role, accessible name, deterministic name-match score, and action metadata, capped at five choices plus an omitted-count marker.
- `make smoke-atspi` validates the daemon-backed focused accessibility tree, accessibility find paths, text-attribute request validation, and deny-by-default accessibility invoke/set-text/insert-text/delete-text/copy-text/cut-text/paste-text/set-caret/set-selection/click-button/set-text-field/focus-text-field/select-menu/activate-tab/activate-link/toggle-check/set-value/select-item paths, and confirms the requests are journaled.
- `crates/plasma-pilotd/tests/protocol_integration.rs` now starts a real daemon binary on a private Unix socket and verifies health, policy, capabilities, and journal-tail responses over the newline JSON protocol.
- `crates/plasma-pilot-cli/tests/daemon_cli_integration.rs` now starts a real daemon binary and verifies that the actual CLI binary can call `doctor`, `capabilities`, `policy-status`, and `journal tail` over a private Unix socket.
- `plasma-pilot-testkit` now provides deterministic mock implementations of the screen, window, input, clipboard, and accessibility backend traits, including call recording for future unit tests that must not touch the real desktop. The screen mock covers raw and scaled screenshots; the input mock covers move, click point/button/count, drag, scroll, text, and key-combo events; the accessibility mock covers focused-tree reads, find requests, invoke calls, set-text calls, insert-text calls, delete-text calls, copy-text calls, cut-text calls, paste-text calls, caret-set calls, selection-set calls, and numeric set-value calls.
- `ScreenshotTransform::output_to_source_point` maps downscaled screenshot output coordinates back to source coordinates, with protocol tests for 8K preview and physical-pixel tile calibration.
- `scripts/gui-eval.sh` and `make gui-eval` run opt-in local evals against a private daemon socket for current non-control flows: status, safety/session preflight metadata, observe, default clipboard-read denial, KWin bridge status metadata, EIS keymap status metadata, bounded screenshot preview metadata, screenshot preview coordinate mapping, configured screenshot preview/tile safety bounds, full-resolution screenshot policy denial, and journal output. On failure, the script reports the artifact directory, daemon log tail, journal tail, and artifact file list before daemon cleanup.
- `scripts/gui-eval.sh session-preflight` and `make gui-eval-session-preflight` validate compact `safety_status` and sanitized `desktop_session_status` metadata plus journal evidence without invoking KDE control, portal prompts, screenshots, clipboard reads, or input.
- `scripts/gui-eval.sh kwin-bridge-status` and `make gui-eval-kwin-bridge-status` validate compact `kwin_bridge_status` metadata and journaling without installing the KWin script or sending input.
- `scripts/gui-eval.sh keymap-status` and `make gui-eval-keymap-status` validate compact `input_backend_status.eis_keymap` source/setup metadata without sending input. When KDE current-layout DBus or `kxkbrc` layout data is visible, the eval checks that the reported keymap source/layout metadata follows the live KDE evidence, and it verifies `input_backend_status` journaling.
- `scripts/gui-eval.sh portal-screenshot` and `make gui-eval-portal-screenshot` now run an explicit opt-in live portal Screenshot eval. It skips when the Screenshot interface is not visible; otherwise it verifies `capture-backends` selects `portal_screenshot`, captures a bounded screenshot and a physical-pixel screenshot tile through the private daemon when the portal returns screenshots, checks output metadata and image existence, and confirms journal provenance for both `screenshot` and `screenshot_tile`. Portal cancellation is treated as a skip unless `PLASMA_PILOT_PORTAL_SCREENSHOT_STRICT=1` is set. This target is not part of `make verify` because xdg-desktop-portal may show a consent dialog.
- `scripts/gui-eval.sh remote-desktop-probe` and `make gui-eval-remote-desktop-probe` run an explicit opt-in live RemoteDesktop consent-path eval. It skips when the RemoteDesktop portal interface or active-window guard metadata is unavailable; otherwise it grants only `remote_desktop_session_probe`, requests keyboard and pointer devices through the private daemon, checks compact response metadata and journaling, and requires a started session only when `PLASMA_PILOT_REMOTE_DESKTOP_STRICT=1` is set. This target is not part of `make verify` because xdg-desktop-portal may show a consent dialog.
- `scripts/gui-eval.sh remote-desktop-eis-session` and `make gui-eval-remote-desktop-eis-session` run an explicit opt-in live retained EIS eval. It skips when the RemoteDesktop portal interface or active-window guard metadata is unavailable, and accepts a cancelled/ended portal session unless `PLASMA_PILOT_REMOTE_DESKTOP_EIS_STRICT=1` is set. When a stored EIS session becomes active, it checks backend status and session metadata, then attempts one minimal scroll and one minimal `Shift` key-combo only after method-scoped `scroll_pointer` and `key_combo` approvals and readiness gates pass; `PLASMA_PILOT_REMOTE_DESKTOP_EIS_INPUT_STRICT=1` requires those input attempts to succeed. This target is not part of `make verify` because it may show a portal dialog and can send real input.
- `scripts/gui-eval.sh control-safety` and `make gui-eval-control-safety` run a private daemon with an isolated panic-stop file and method-scoped approval-file grant, seed active-window state through the daemon KWin bridge when possible, and verify wrong-window guard denial plus panic-stop denial before backend control execution. The control-safety eval is included in `make verify`.
- `ReplayTrace`, `plasma-pilot-cli trace validate --file <path>`, `plasma-pilot-cli trace validate --dir <path>`, `plasma-pilot-cli trace replay --file <path>`, and `plasma-pilot-cli trace replay --dir <path>` provide replayable daemon-request traces with expected response metadata, including optional error-message substring expectations and JSON-pointer equality, type, and existence checks for compact response-field assertions. Validation checks trace version, non-empty/unique labels, expected response types, JSON-pointer syntax, supported JSON value types, and contradictory error expectations without contacting the daemon; directory validation/replay reject empty trace sets and return compact aggregate reports. Replayed steps still flow through daemon policy enforcement and journaling, and mismatch errors include step index, label, method, and expected/actual details without echoing full response payloads. `examples/traces/status-smoke.json` is a checked-in status-only replay fixture and now covers KWin bridge diagnostics, uinput access diagnostics, capture backend diagnostics, `input_backend_status.eis_keymap` metadata, and inactive RemoteDesktop EIS session status/stop responses, `examples/traces/policy-denials-smoke.json` is a safe deny-by-default fixture for protected screenshot, clipboard-read, focus-control, and AT-SPI caret/selection control actions, `examples/traces/input-denials-smoke.json` verifies raw keyboard, pointer, RemoteDesktop session probe, RemoteDesktop EIS probe, and RemoteDesktop EIS start requests fail closed before backend input side effects, and `examples/traces/panic-stop-smoke.json` verifies panic-stop state transitions through response-field assertions; all are covered by real CLI integration tests, `make validate-traces`, `make smoke-trace-replay`, and the standard `make verify` gate.
- `plasma-pilot-cli panic-stop status|enable|disable` now controls a daemon-side file-backed panic-stop flag. The daemon journals these requests and rejects control-class actions while the flag is active, even when control policy is explicitly allowed.
- MCP now exposes `plasma.panic_stop_status`, `plasma.panic_stop_enable`, and `plasma.panic_stop_disable` through the same daemon-backed panic-stop protocol.
- `plasma-pilot-cli journal tail` and MCP `plasma.journal_tail` now support method and success filters for compact troubleshooting, for example `--method focus_window --ok false`.
- Current control requests now support optional active-window guards. CLI/MCP callers can pass expected active window id, app id, or title substring; the daemon rejects the request before execution if the active window no longer matches.
- `[safety].require_focus_guard` now defaults to true, so control-class requests require an active-window guard by default unless a scoped local development config explicitly opts out.
- `[safety].control_rate_limit_per_minute` now defaults to 120 accepted control-class requests over a rolling 60-second window. Set it to 0 only for scoped local development; observe/status requests are unaffected.
- `plasma-pilot-cli safety-status` and MCP `plasma.safety_status` now expose a compact read-only safety preflight: focus-guard enforcement, human-input pause state, fresh activity-signal state, quiet interval, optional signal path, control rate limit, screenshot preview/tile max-edge defaults, and screenshot redaction count.
- `plasma-pilot-cli desktop-session-status` and MCP `plasma.desktop_session_status` now expose sanitized KDE/Wayland/session environment diagnostics for DBus, runtime-directory, KWin, portal, and AT-SPI troubleshooting without returning raw session bus or runtime paths. Daemon capabilities advertise this as `daemon_desktop_session_status`.
- `plasma-pilot-cli wait-for-change` and MCP `plasma.wait_for_change` now poll bounded screenshots, compute normalized RGB delta against a baseline, and return compact changed/captures/elapsed/score metadata with the latest screenshot metadata.
- Screenshot and wait-for-change compact daemon/MCP summaries now include capture backend provenance, matching the structured `ScreenshotInfo.backend` field and the project provenance rule without storing screenshot payloads in the journal.
- `plasma-pilot-cli kwin-bridge-status` and MCP `plasma.kwin_bridge_status` now report daemon DBus receiver state, whether a bridge update has been seen, and user-local KWin script package/config installation state.
- `plasma-pilot-uinput` now provides an initial Linux uinput keyboard backend. `plasma-pilot-cli input type-text`, `plasma-pilot-cli input key-combo`, MCP `plasma.type_text`, and MCP `plasma.key_combo` send policy-gated `ControlKeyboard` requests through the daemon; panic-stop and active-window guards apply, and summaries report text length or key count only.
- `plasma-pilot-uinput` now provides an initial Linux uinput pointer backend. `plasma-pilot-cli input move-pointer`, `click-pointer`, `drag-pointer`, and `scroll-pointer`, plus MCP `plasma.move_pointer`, `plasma.click_pointer`, `plasma.drag_pointer`, and `plasma.scroll_pointer`, send policy-gated `ControlPointer` requests through the daemon; panic-stop and active-window guards apply. Move/click/drag require explicit coordinate space. `physical_pixel` coordinates validate against monitor-derived physical bounds for scaled 8K layouts, while guarded active-window `window_local` coordinates resolve through active-window logical geometry and monitor scale before uinput execution. Click supports one or two left/middle/right clicks, drag supports bounded press-move-release with left/middle/right buttons, and scroll supports vertical/horizontal wheel deltas.
- `plasma-pilot-cli input status`, MCP `plasma.uinput_status`, and `make smoke-uinput-status` now provide safe uinput access diagnostics without sending input. The status reports `/dev/uinput` availability, character-device state, mode/owner ids, daemon effective uid/gid, and a setup hint. `docs/uinput-setup.md` documents the optional udev rule, user service setup, current polkit placeholder state, and safe smoke flow.
- `plasma-pilot-cli input backends`, MCP `plasma.input_backend_status`, and `make smoke-uinput-status` now provide safe portal/libei/uinput backend probes without starting a RemoteDesktop session or sending input. The probe reports xdg-desktop-portal RemoteDesktop interface visibility, KDE portal service visibility, libei client metadata/socket hints, uinput fallback availability, the configured backend request, the preferred visible backend, the currently implemented backend, and compact EIS keymap source/layout metadata. `[backends].input`, `--input-backend`, and `PLASMA_PILOT_INPUT_BACKEND` can request `auto`, `uinput`, `portal_remote_desktop`, or `libei`; raw keyboard/pointer commands route through a daemon input-executor trait. `auto` and `uinput` use the uinput executor today. Explicit portal/libei selections build EIS action plans for text, named evdev key combos, and pointer requests, then execute through the stored daemon EIS session only after the per-plan readiness gate passes; without a stored session or ready selected device they fail closed before side effects.
- `plasma-pilot-portal` now codifies the official xdg-desktop-portal RemoteDesktop request/session/EIS contract as a tested crate boundary: service/object/interface constants, keyboard/pointer/touchscreen device bitmasks, persist modes, `CreateSession`, `SelectDevices`, `Start`, and `ConnectToEIS` bus-call builders, Request and Session handle-token/path validation, session-handle response parsing, start-response device/clipboard/restore-token parsing, a mockable lifecycle/EIS transport, a zbus lifecycle that can drive `CreateSession -> SelectDevices -> Start` while pre-subscribing to expected Request responses, and same-connection zbus `ConnectToEIS` helpers that return an owned FD. The daemon can wrap a portal-returned EIS FD in transient probe runtimes and retained daemon-owned EIS sessions; explicit portal/libei raw input uses the retained session only after policy, active-window, panic-stop, and readiness gates pass.
- `plasma-pilot-cli input remote-desktop-probe` and MCP `plasma.remote_desktop_session_probe` now provide an explicit, policy-gated transient RemoteDesktop lifecycle probe. It can request keyboard/pointer/touchscreen devices, optional restore token and persist mode, and optional active-window guard; it may show a portal consent dialog, reports selected devices/clipboard/restore metadata and request/session handles, then closes the transient session without calling `ConnectToEIS` or sending Notify*/EIS input.
- `plasma-pilot-cli input remote-desktop-eis-probe` and MCP `plasma.remote_desktop_eis_probe` now provide an explicit, policy-gated transient RemoteDesktop EIS probe. It requests the same consented session, calls `ConnectToEIS` after `Start` on the same DBus connection, initializes a transient daemon EIS runtime from the returned FD, polls pending events, closes the runtime, reports only compact metadata plus runtime connected/event/bound-capability/resumed-device counts, and sends no EIS or Notify* input.
- `plasma-pilot-cli input remote-desktop-eis-start`, `plasma-pilot-cli input remote-desktop-eis-session-status`, `plasma-pilot-cli input remote-desktop-eis-stop`, MCP `plasma.remote_desktop_eis_start`, MCP `plasma.remote_desktop_eis_session_status`, and MCP `plasma.remote_desktop_eis_stop` now expose the daemon-owned RemoteDesktop EIS session lifecycle. Start is policy-gated control and may open a portal dialog; status and stop are safe lifecycle diagnostics. These commands retain or drop the session and report compact runtime/device readiness metadata. Raw input is sent only by separate policy-gated control requests configured for portal/libei, and only after the stored-session readiness gate passes.
- Raw keyboard and pointer command handlers now use a daemon `InputExecutionBackend` trait boundary. `auto` and `uinput` use the uinput adapter; portal RemoteDesktop and libei preferences route EIS plans through the stored daemon EIS session and still fail closed before side effects when no stored session or ready selected device exists. Successful action summaries include backend provenance such as `backend=uinput` or `backend=portal_remote_desktop`.
- `plasma-pilot-eis` now models libei sender event plans used by stored-session EIS input execution. It covers UTF-8 text and XKB-compatible text keysyms through the text capability, named evdev key combos through the keyboard capability, absolute pointer movement, click, drag, and discrete scroll; each plan includes start/stop emulation boundaries, frames, required capabilities, Linux input-event-codes key/button values, XKB keysym conversion through xkbcommon where applicable, and 120-unit scroll steps. It also exposes an `EisEventSink` abstraction and a `LibeiDeviceSink` FFI adapter that calls libei sender APIs for a caller-owned resumed device while rejecting interior-NUL text before crossing FFI. Tested device selection now requires a resumed device with every plan capability and, for absolute pointer plans, a virtual-device region covering all target coordinates; paused devices, missing capabilities, out-of-region targets, cross-region drags, and physical absolute devices fail closed. `LibeiSenderContext` can take ownership of a portal-returned EIS FD, configure the libei sender name, expose the libei event FD for polling, dispatch pending events, bind the intersection of plan-required and seat-available capabilities on `SeatAdded`, snapshot connect/seat/device/resume/pause/remove events into compact device metadata, retain refcounted resumed device handles until pause/remove/seat removal/disconnect, and is marked `Send` for daemon mutex-serialized ownership transfer. `EisRuntimeState` consumes those snapshots, tracks connection/seat/bound-capability state plus the current device list, applies pause/remove/disconnect events, selects a resumed device for a plan from live-style state, and now reports stricter execution readiness only when the session is connected, all plan-required capabilities are bound, and a resumed matching device is selected. `EisSessionRuntime` wraps an event source plus runtime state so a daemon-owned EIS session can poll pending events, poll plan-aware seat bindings, report planning or execution readiness, and hand a ready plan to a selected-device executor only after readiness passes. The live `LibeiSenderContext` selected-device executor applies validated plans only to retained selected devices and fails closed if the selected device is no longer retained. This selected-device executor boundary is covered by mock tests and is now used by explicit portal/libei raw input when a stored EIS session is active and ready. The daemon now has a `DaemonPortalEisSession` wrapper that preserves portal session metadata while owning an EIS runtime, a mutex-backed single-session store with start/status/stop daemon protocol, plus a tested session-backed input executor that turns text/keyboard/pointer requests into EIS plans and calls the ready selected-device executor only after readiness passes. The transient EIS probe initializes a session wrapper, polls, drops it, and still sends no input. Explicit portal/libei raw-input selections now use the stored daemon EIS session after policy, panic-stop, active-window guard, and per-plan readiness checks. CLI/MCP wrappers expose stored-session start, status, and stop. The EIS crate now also owns a tested xkbcommon keymap wrapper that builds explicit RMLVO keymaps, maps level-0 keysyms to XKB keycodes, and converts those to evdev keycodes with the documented 8-code offset; explicit EIS key combos now preserve named evdev parsing first and then use configured `[backends.keymap]` RMLVO names, KDE current-layout DBus metadata, KDE `kxkbrc` config via `kreadconfig6`, or xkbcommon defaults for unsupported single-character symbol parts.
- `plasma-pilot-cli capture-backends`, MCP `plasma.capture_backend_status`, and `make smoke-capture-backends` now provide safe capture backend probes without starting a portal session or capturing pixels. The probe reports xdg-desktop-portal Screenshot/ScreenCast interface visibility, KWin supportInformation metadata availability, Spectacle fallback availability, the preferred visible capture backend, and the currently implemented capture backend. When portal Screenshot is visible, full-screen screenshot execution uses the portal through the tested `plasma-pilot-portal` zbus transport; tile capture now uses the same portal source before cropping/downscaling the requested physical-pixel region. Spectacle remains the compatibility fallback when portal Screenshot is unavailable or fails before a user response.
- `plasma-pilot-portal` now codifies the official xdg-desktop-portal Screenshot request contract as a tested crate boundary: service/object/interface constants, handle-token validation, expected and returned Request object paths, Response signal match rules, response-code handling, screenshot URI extraction, file URI decoding, a transport trait that drives the request lifecycle, and a zbus execution path used by the daemon for full-screen portal screenshots.
- `plasma-pilot-cli input pointer-calibration`, MCP `plasma.pointer_calibration`, and `make smoke-pointer-calibration` now provide safe pointer calibration diagnostics without moving the pointer. The response reports physical-pixel bounds derived from KWin monitor metadata, per-monitor physical origins, and representative top-left/center/bottom-right sample points for guarded GUI smoke.
- `docs/arch-kde-install.md` now provides the consolidated Arch Linux + KDE Plasma 6 operator install runbook, including package prerequisites, binary installation, user service setup, KWin bridge enablement, safe diagnostics, optional uinput, Codex plugin validation, approval flow, and troubleshooting.
- `make smoke-gui-input` now validates real policy-gated uinput control in a disposable KWrite/Kate document. It starts a private daemon with an approval file, grants only the focus, click, type, and save methods it uses, focuses the test window through KWin, requires an active-window guard, maps a safe window point to physical pixels through pointer calibration, clicks, types a sentinel, saves, verifies file content, and checks the journal.
- The host GUI smoke exposed and fixed the initial keyboard mapping bug where text typing assumed evdev letter codes were contiguous; the uinput backend now uses explicit US evdev letter mappings.
- AT-SPI run-attribute mutation if a supported interface is identified, KWin-native capture backends, broader live KDE evals, and portal clipboard integration if a stable interface appears remain future work.
