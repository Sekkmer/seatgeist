# Live evaluation fixtures

These fixtures are disposable local targets for opt-in Seatgeist live KDE
acceptance runs. They contain no credentials or external network dependency.

`firefox/index.html` exposes a continuously changing frame counter and one safe
accessible button. Copy `firefox-profile/` to a private generated directory
before launching Firefox; do not run a real browser profile for the eval.
